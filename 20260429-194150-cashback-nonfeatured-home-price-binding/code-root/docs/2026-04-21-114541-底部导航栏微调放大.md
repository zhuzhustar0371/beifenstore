# 底部导航栏微调放大

- 操作时间：2026-04-21 11:45:41 +08:00
- 操作人：Codex
- 任务状态：已获批准，正在执行

## 1. 变更目标

- 在当前胶囊导航方案基础上，将底部导航栏整体稍微调大一点。
- 保持当前玻璃质感、布局逻辑和文字大小基本不变。

## 2. 分析结论

- 用户要求的是“微调放大”，不需要重做布局。
- 最稳妥的方式是联动放大：
  - `.liquid-tabbar` 高度
  - `.tab-bubble` 圆圈尺寸
  - `.tab-icon` 图标尺寸
- 文案字号先保持不变，避免视觉发胀。

## 3. 批准记录

- 用户已明确批准本次微调放大。

## 4. 安全备份

- 本地独立备份目录：
  - `g:\zhiximini\_local_backups\2026-04-21-114541-tabbar-slight-enlarge`
- 已备份文件：
  - `g:\zhiximini\wechat-app\custom-tab-bar\index.wxss`
- 备份副本：
  - `g:\zhiximini\_local_backups\2026-04-21-114541-tabbar-slight-enlarge\index.wxss.bak`

## 5. 计划修改文件

- `g:\zhiximini\wechat-app\custom-tab-bar\index.wxss`

## 6. 计划修改内容

- 轻微增大 `.liquid-tabbar` 高度与圆角。
- 轻微增大 `.tab-bubble` 和 `.tab-icon` 尺寸。
- 保持当前文字字号不变。

## 7. 影响范围

- 仅影响底部导航栏整体视觉尺寸。
- 不影响路由、业务逻辑、接口和数据。

## 8. 回退方案

- 如效果不符合预期，直接使用以下备份文件恢复：
  - `g:\zhiximini\_local_backups\2026-04-21-114541-tabbar-slight-enlarge\index.wxss.bak`

## 9. 执行结果

- 日志文件已创建。
- 样式文件已完成备份。
- 代码修改：已完成。

## 10. 实际修改内容

- `g:\zhiximini\wechat-app\custom-tab-bar\index.wxss`
  - `.liquid-tabbar`
    - 高度从 `100rpx` 调整为 `106rpx`
    - 圆角从 `50rpx` 调整为 `53rpx`
  - `.tab-bubble`
    - 尺寸从 `48rpx * 48rpx` 调整为 `50rpx * 50rpx`
    - 圆角从 `24rpx` 调整为 `25rpx`
  - `.tab-icon`
    - 尺寸从 `24rpx * 24rpx` 调整为 `25rpx * 25rpx`

## 11. 调整说明

- 本次只做一档很小的放大。
- 文案字号与行高保持不变，避免视觉发胀。
- 目标是让底部导航栏更舒展一点，但不改变当前布局风格。

## 12. 验证记录

- 本次仅修改 `index.wxss`。
- 未修改 `index.wxml`、`index.js`、路由和业务逻辑。

## 13. 回退指引

- 如需恢复本次修改前版本，可直接使用以下备份文件覆盖：
  - `g:\zhiximini\_local_backups\2026-04-21-114541-tabbar-slight-enlarge\index.wxss.bak`
