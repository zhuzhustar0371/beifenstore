# 我的页面规则说明图标替换日志

## 1. 目标

将小程序「我的」页面中「规则说明」入口的圆形渐变占位块，替换为用户提供的正式图标资源，并保持图标按正确比例显示。

## 2. 现状分析

### 2.1 页面位置

- 入口位于 `[wechat-app/pages/user/user.wxml](/G:/zhiximini/wechat-app/pages/user/user.wxml)` 的 `action-grid` 最后一项。
- 样式位于 `[wechat-app/pages/user/user.wxss](/G:/zhiximini/wechat-app/pages/user/user.wxss)`。

### 2.2 当前实现

- 前三个入口已经在上一轮改成了图片图标。
- 「规则说明」入口仍然使用一个圆形渐变块：
  - 宽高 64rpx
  - 圆角 32rpx
  - 纯背景色，不是真实图片

### 2.3 本次处理方式

- 使用你提供的 `icons8-规则-100.png` 作为正式图标资源。
- 将图标复制到项目内 `wechat-app/images/`。
- 页面上使用 `aspectFit`，避免图标被拉伸或裁切。

## 3. 修改前备份

### 3.1 本地备份

已保存修改前快照到：

- `_local_backups/2026-04-22-025457-user-rule-icon-replace/wechat-app/pages/user/user.wxml`
- `_local_backups/2026-04-22-025457-user-rule-icon-replace/wechat-app/pages/user/user.wxss`
- `_local_backups/2026-04-22-025457-user-rule-icon-replace/wechat-app/images/README.md`

### 3.2 说明

- 本次备份只覆盖本次会动到的页面和资源说明文件。
- 仓库中其它未提交变更保持不动，没有回退、没有覆盖。

## 4. 图标资源

### 4.1 来源文件

- `C:\Users\朱鑫\Downloads\icons8-规则-100.png`

### 4.2 项目内文件

- `[wechat-app/images/rule-icon.png](/G:/zhiximini/wechat-app/images/rule-icon.png)`

### 4.3 视觉说明

- 图标本身是方形文件，内部为规则样式线稿。
- 使用 `aspectFit` 后，图标会在 64rpx 入口容器内等比缩放。
- 不会强行裁成圆形，保留图标原始轮廓。

## 5. 实际修改

### 5.1 页面结构

修改文件：

- `[wechat-app/pages/user/user.wxml](/G:/zhiximini/wechat-app/pages/user/user.wxml)`

修改内容：

- 将「规则说明」入口从 `<view class="grid-icon rule-icon"></view>` 改为：
  - `<image class="grid-icon grid-icon-image" src="/images/rule-icon.png" mode="aspectFit" />`

### 5.2 样式层

修改文件：

- `[wechat-app/pages/user/user.wxss](/G:/zhiximini/wechat-app/pages/user/user.wxss)`

修改内容：

- 保留 `.grid-icon` 作为统一容器尺寸和布局基类。
- 保留 `.grid-icon-image` 的透明背景处理。
- 删除已不再使用的 `.rule-icon` 渐变背景样式，避免样式残留误导后续维护。

### 5.3 资源说明文档

修改文件：

- `[wechat-app/images/README.md](/G:/zhiximini/wechat-app/images/README.md)`

修改内容：

- 新增 `rule-icon.png` 的资源说明和建议尺寸。

## 6. 验证结果

### 6.1 结构检查

已确认页面中四个入口现在分别是：

- 我的订单
- 收益明细
- 邀请海报
- 规则说明

其中前三个和本次这个规则入口都采用图片图标，显示模式统一为 `aspectFit`。

### 6.2 资源检查

已确认项目内存在：

- `wechat-app/images/order-icon.png`
- `wechat-app/images/cashback-icon.png`
- `wechat-app/images/invite-icon.png`
- `wechat-app/images/rule-icon.png`

## 7. 回滚方式

若需要回退本次修改，可直接恢复下面的备份文件：

- `_local_backups/2026-04-22-025457-user-rule-icon-replace/wechat-app/pages/user/user.wxml`
- `_local_backups/2026-04-22-025457-user-rule-icon-replace/wechat-app/pages/user/user.wxss`
- `_local_backups/2026-04-22-025457-user-rule-icon-replace/wechat-app/images/README.md`

同时可以将 `wechat-app/images/rule-icon.png` 删除或替换回旧方案。

## 8. 备注

- 本次修改只影响「我的」页面入口图标显示，不影响登录、订单、收益、邀请或客服跳转逻辑。
- 如果你后面还想微调图标大小，我可以继续把 `64rpx` 容器和内边距做更细的视觉校准。
