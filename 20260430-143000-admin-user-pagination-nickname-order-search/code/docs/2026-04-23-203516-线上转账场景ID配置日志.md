# 线上 WECHAT_PAY_TRANSFER_SCENE_ID 配置日志

## 基本信息

- 操作时间：2026-04-23 20:35 CST
- 操作人：Codex
- 服务器：ubuntu@43.139.76.37
- 后端应用目录：/home/ubuntu/apps/backend-api
- 后端环境变量文件：/home/ubuntu/apps/backend-api/.env
- 后端服务：zhixi-backend.service
- 本次目标：将线上 `WECHAT_PAY_TRANSFER_SCENE_ID` 配置为用户批准的已开通场景 ID `1000`，用于微信商家转账返现打款。

## 变更前分析

- 当前返现打款接口已切换到微信支付新版商家转账接口。
- 线上报错曾出现：`INVALID_REQUEST - 你尚未获取该转账场景`。
- 该错误说明后端传给微信支付的 `transfer_scene_id` 未在商户平台获得对应权限。
- 本次操作不改代码、不重新构建，只修改线上环境变量并重启后端服务，使运行时读取 `WECHAT_PAY_TRANSFER_SCENE_ID=1000`。
- 风险说明：如果商户平台实际没有开通场景 `1000`，微信支付仍会返回“尚未获取该转账场景”；此时需要在微信支付商户平台开通该场景，或改成平台已开通的其他场景 ID。

## 用户批准

- 用户明确批准：`批准配置线上 WECHAT_PAY_TRANSFER_SCENE_ID=1000`

## 备份记录

- 备份对象：/home/ubuntu/apps/backend-api/.env
- 备份文件：/home/ubuntu/apps/backend-api/backups/.env-before-transfer-scene-20260423203516
- 备份方式：执行写入前使用 `cp -p` 保留原文件权限和时间属性。

## 执行步骤

1. 在服务器创建备份目录：`/home/ubuntu/apps/backend-api/backups`
2. 备份线上环境变量文件：`/home/ubuntu/apps/backend-api/.env`
3. 在 `.env` 中插入或替换配置：`WECHAT_PAY_TRANSFER_SCENE_ID=1000`
4. 重启后端服务：`sudo systemctl restart zhixi-backend.service`
5. 首次立即检查 `http://127.0.0.1:8080/api/health` 时出现短暂 `Connection refused`，随后确认 systemd 服务为 `active (running)`，判断为服务启动窗口期。
6. 重试健康检查，内网健康检查成功。
7. 执行公网健康检查，`https://api.mashishi.com/api/health` 返回 UP。
8. 检查管理后台首页，`https://admin.mashishi.com/` 返回 HTTP 200。

## 变更结果

- 当前线上环境变量：`WECHAT_PAY_TRANSFER_SCENE_ID=1000`
- 后端服务状态：`active`
- 内网健康检查：`{"success":true,"message":"OK","data":{"status":"UP"}}`
- 公网健康检查：`{"success":true,"message":"OK","data":{"status":"UP"}}`
- 管理后台检查：`HTTP/1.1 200 OK`
- 是否触发回退：否

## 变更文件

- 远端修改：/home/ubuntu/apps/backend-api/.env
- 远端备份：/home/ubuntu/apps/backend-api/backups/.env-before-transfer-scene-20260423203516
- 本地日志：G:\zhiximini\docs\2026-04-23-203516-线上转账场景ID配置日志.md

## 回退方案

如配置后微信支付仍报错或后端服务异常，可执行以下回退：

```bash
ssh ubuntu@43.139.76.37
cd /home/ubuntu/apps/backend-api
cp -p backups/.env-before-transfer-scene-20260423203516 .env
sudo systemctl restart zhixi-backend.service
sudo systemctl is-active zhixi-backend.service
curl -fsS http://127.0.0.1:8080/api/health
```

## 后续验证建议

- 在管理后台对之前因“尚未获取该转账场景”失败的返现单点击“新版重试”或“批准打款”。
- 如果仍返回“尚未获取该转账场景”，说明商户平台没有开通 `1000`，需要到微信支付商户平台产品中心开通“商家转账”对应场景，或把本变量改成商户平台显示的已开通场景 ID。
