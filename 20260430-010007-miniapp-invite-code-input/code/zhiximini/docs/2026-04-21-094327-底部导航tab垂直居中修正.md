# 底部导航 tab 垂直居中修正

- 操作时间：2026-04-21 09:43:27 +08:00
- 操作人：Codex
- 任务状态：已获批准，正在执行

## 1. 变更目标

- 让微信小程序底部自定义导航栏中的两个 `tab-item` 相对于导航栏胶囊容器实现真正的垂直居中。
- 修正此前“改了尺寸但位置看起来几乎没动”的问题。

## 2. 问题分析

- 当前 `首页` / `我的` 都位于 `tab-item` 内部，位置主要由 `.liquid-tabbar` 与 `.tab-item` 的纵向布局共同决定。
- 之前的修改主要调整了尺寸与间距，没有直接调整 `tab-item` 在导航栏容器中的垂直定位。
- `.tab-bubble-active` 的上浮效果会进一步破坏视觉中线，导致用户感知为“没有居中”。

## 3. 批准记录

- 用户已明确批准本次“垂直居中修正”。

## 4. 安全备份

- 本地独立备份目录：
  - `g:\zhiximini\_local_backups\2026-04-21-094327-tabbar-vertical-center`
- 已备份文件：
  - `g:\zhiximini\wechat-app\custom-tab-bar\index.wxss`
- 备份副本：
  - `g:\zhiximini\_local_backups\2026-04-21-094327-tabbar-vertical-center\index.wxss.bak`

## 5. 计划修改文件

- `g:\zhiximini\wechat-app\custom-tab-bar\index.wxss`

## 6. 计划修改内容

- 调整 `.liquid-tabbar` 的纵向对齐方式和可用内容高度。
- 让 `.tab-item` 占满导航栏主要内容区，并保持严格垂直居中。
- 去除或收敛破坏中线的激活态位移。
- 保持图标、文字、路由逻辑不变。

## 7. 影响范围

- 仅影响微信小程序底部自定义导航栏的垂直居中效果。
- 不影响业务逻辑、页面跳转、接口与数据。

## 8. 回退方案

- 如效果不符合预期，直接使用以下备份文件恢复：
  - `g:\zhiximini\_local_backups\2026-04-21-094327-tabbar-vertical-center\index.wxss.bak`

## 9. 执行结果

- 日志文件已创建。
- 样式文件已完成备份。
- 代码修改：已完成。

## 10. 实际修改内容

- `g:\zhiximini\wechat-app\custom-tab-bar\index.wxss`
  - `.liquid-tabbar`
    - `align-items` 从 `center` 调整为 `stretch`
    - `padding-top` 从 `10rpx` 调整为 `8rpx`
  - `.tab-item`
    - `min-height` 从 `64rpx` 调整为 `72rpx`
    - 新增 `height: 100%`
    - 保持 `justify-content: center`
  - `.tab-bubble-active`
    - 去掉上浮位移：从 `translateY(-2rpx) scale(1.01)` 调整为 `scale(1.01)`

## 11. 修改效果

- 两个 `tab-item` 现在会在导航栏内容区内拉伸占位，再由自身居中对齐。
- 激活态不再额外向上漂移，避免破坏视觉中线。
- 这次调整针对的是相对于导航栏容器的垂直居中，而不是单纯改元素大小。

## 12. 验证记录

- 已复核修改后的 `index.wxss`，确认本次仍只修改一个样式文件。
- 未执行构建、推送、发布；本次按用户请求仅完成本地样式修正与留档。

## 13. 回退指引

- 如需恢复到本次修改前版本，可直接覆盖：
  - 源文件：`g:\zhiximini\wechat-app\custom-tab-bar\index.wxss`
  - 备份文件：`g:\zhiximini\_local_backups\2026-04-21-094327-tabbar-vertical-center\index.wxss.bak`
