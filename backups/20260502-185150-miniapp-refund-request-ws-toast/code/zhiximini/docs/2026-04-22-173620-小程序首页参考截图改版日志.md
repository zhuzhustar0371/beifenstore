# 2026-04-22 小程序首页参考截图改版日志

## 1. 任务信息

- 时间：2026-04-22 17:36:20 +08:00
- 操作人：Codex
- 工作目录：`G:\zhiximini`
- 任务目标：把微信小程序首页样式调整为用户提供截图对应的视觉风格，重点对齐首页首屏商品卡片、会员权益说明卡片、底部悬浮导航栏。
- 执行前约束：
  - 已先进行代码结构分析。
  - 已获得用户批准后开始修改。
  - 本次只处理小程序前端页面样式与结构，不调整后端接口、不改商品数据来源。

## 2. 变更前分析结论

- 首页页面文件位于：
  - `G:\zhiximini\wechat-app\pages\index\index.wxml`
  - `G:\zhiximini\wechat-app\pages\index\index.wxss`
  - `G:\zhiximini\wechat-app\pages\index\index.js`
- 底部导航为自定义 tabBar，文件位于：
  - `G:\zhiximini\wechat-app\custom-tab-bar\index.wxml`
  - `G:\zhiximini\wechat-app\custom-tab-bar\index.wxss`
- 参考图与现状主要差异：
  - 现状首页顶部为绿色 Hero Banner，参考图为大面积白底留白。
  - 现状商品区带“优选推荐”标题，参考图为直接展示大白卡商品。
  - 现状会员权益区只有文案骨架，缺少参考图对应的卡片、步骤条、分割线、文案层级和阴影细节。
  - 现状底部导航偏玻璃态，参考图更接近纯白胶囊悬浮栏。

## 3. 备份记录

- 备份时间：2026-04-22 17:36:20 +08:00
- 备份目录：`G:\zhiximini\_local_backups\20260422-173620`
- 备份内容：
  - `wechat-app\pages\index\*`
  - `wechat-app\custom-tab-bar\*`
- 备份目的：
  - 保留本次首页改版前的局部稳定副本，便于快速恢复首页与底部导航样式。

## 4. 本地修改记录

### 4.1 首页结构改造

- 修改文件：`G:\zhiximini\wechat-app\pages\index\index.wxml`
- 修改内容：
  - 删除原有绿色 Hero Banner 结构。
  - 新增顶部浅绿色装饰条与分隔层，贴近截图顶部视觉节奏。
  - 取消“优选推荐”标题区，改为直接展示商品大卡片列表。
  - 调整商品卡片结构为“左图 + 右文案 + 底部价格 + 绿色圆形加号”。
  - 将会员权益区域改造成大白卡，包含：
    - 标题区左侧绿色竖条；
    - 价格摘要卡片；
    - 三步免单步骤条；
    - 分享收益说明列表；
    - 底部品牌 slogan。

### 4.2 首页样式重做

- 修改文件：`G:\zhiximini\wechat-app\pages\index\index.wxss`
- 修改内容：
  - 重设页面背景为白色到浅灰蓝的轻渐变。
  - 商品卡片改为大圆角白卡、轻阴影、较大的图片容器与更明显的价格层级。
  - 新增热卖与优选角标样式。
  - 新增会员权益卡片全部样式，包括摘要卡、步骤卡、分隔线、收益说明、底部口号。
  - 补充小屏与大屏下的自适应样式，避免不同机型下卡片比例失衡。

### 4.3 自定义底部导航视觉收敛

- 修改文件：`G:\zhiximini\wechat-app\custom-tab-bar\index.wxss`
- 修改内容：
  - 将底部导航从半透明玻璃感调整为更接近截图的纯白胶囊容器。
  - 弱化高光与彩色 glow，保留干净的悬浮阴影。
  - 调整激活态图标容器为白色圆形浮层。
  - 调整标签字号、激活色和整体留白，使“首页 / 我的”更接近截图比例。

## 5. 本次未修改内容

- 未修改 `pages/index/index.js` 的接口请求逻辑。
- 未修改商品接口地址与数据结构。
- 未修改后端服务、数据库、支付、登录逻辑。
- 未执行任何线上发布或回滚动作。

## 6. 验证记录

- 验证方式：
  - 人工对照参考截图逐项核对页面结构和样式层级。
  - 检查 WXML/WXSS 改动是否保持现有数据绑定与点击跳转逻辑不变。
  - 复核最终 `git diff` 输出，确认修改集中在首页结构、首页样式和自定义 tabBar 样式三个区域。
- 已验证点：
  - 商品列表仍使用原有 `products` 数据源渲染。
  - 商品点击仍绑定 `goProduct` 跳转详情页。
  - 自定义 tabBar 结构未改，只调整视觉层样式，功能逻辑保持原样。
  - WXML 标签闭合完整，未发现明显结构性错误。
  - WXSS 中保留原项目已在使用的响应式写法，同时将角标 `display` 从 `inline-flex` 调整为 `flex`，降低小程序样式兼容风险。
- 未完成项：
  - 当前会话未直接拉起微信开发者工具，因此尚未完成真机/模拟器视觉验收截图。

## 7. 云端构建与发布状态

- 本次状态：未执行。
- 原因：
  - 当前任务是本地首页样式改版。
  - 当前回合未收到发布上线指令，也未执行云端流水线构建。
  - 因未发布，故也未触发线上异常回退。

## 8. 回退方案

- 如需撤回本次首页改版，可从以下备份目录恢复：
  - `G:\zhiximini\_local_backups\20260422-173620`
- 推荐回退范围：
  - `wechat-app\pages\index\*`
  - `wechat-app\custom-tab-bar\*`

## 9. 变更文件清单

- `G:\zhiximini\wechat-app\pages\index\index.wxml`
- `G:\zhiximini\wechat-app\pages\index\index.wxss`
- `G:\zhiximini\wechat-app\custom-tab-bar\index.wxss`
- `G:\zhiximini\docs\2026-04-22-173620-小程序首页参考截图改版日志.md`

## 10. 后续建议

- 在微信开发者工具中打开 `wechat-app` 项目进行首页视觉核对，重点检查：
  - 商品图比例；
  - 价格字号；
  - 底部导航在 iPhone 与 Android 机型下的悬浮位置；
  - 长商品名或长描述是否出现换行挤压。
- 若用户要求继续“完全一比一”逼近截图，可在下一轮进一步微调：
  - 顶部留白高度；
  - 商品卡片左右比例；
  - 会员权益卡片内部行高；
  - 底部 tabBar 宽度与阴影半径。
