# 地址表旧字段兼容修复发布日志

- 时间: 2026-04-21 19:44:11
- 操作人: Codex
- 任务: 修复小程序地址页点击后接口 `GET /api/addresses` 返回 500 的问题，并发布上线
- 用户批准: 已批准修复并发布

## 一、问题分析

- 现象: 小程序点击地址页仍报错，控制台显示 `GET https://api.mashishi.com/api/addresses 500`
- 前端状态: 未登录访问已返回 `400 请先登录`，前端未登录拦截逻辑已生效
- 后端日志: `Unknown column 'recipient_name' in 'field list'`
- 根因: 线上 `user_addresses` 仍为旧表结构，字段为 `receiver_name`、`receiver_mobile`，当前后端代码使用 `recipient_name`、`recipient_phone`
- 原因细化: `DatabaseMigrationRunner` 只确保表存在，未兼容已存在旧表的字段升级

## 二、执行前备份

- 本地备份目录: `G:\zhiximini\_local_backups\2026-04-21-194411-address-schema-fix`
- 已备份文件:
  - `backend-api/src/main/java/com/zhixi/backend/config/DatabaseMigrationRunner.java`

## 三、计划执行步骤

1. 修改后端地址表迁移逻辑，兼容旧字段名
2. 本地构建后端 jar 并验证
3. 备份服务器当前 jar
4. 发布新 jar 并重启服务
5. 验证健康接口和地址接口
6. 如失败，立即回退服务器备份版本

## 四、本地修改记录

- [19:44:11] 已创建本地备份与日志文档
- [19:45:19] 已修改 `backend-api/src/main/java/com/zhixi/backend/config/DatabaseMigrationRunner.java`
- 修改内容:
  - 新增旧字段重命名迁移: `receiver_name -> recipient_name`
  - 新增旧字段重命名迁移: `receiver_mobile -> recipient_phone`
  - 新增 `information_schema.columns` 列存在性检测
  - 新增地址新字段兜底补列逻辑

## 五、构建记录

- [19:45:47] 首次执行 `mvn -DskipTests package`
- 结果: 构建成功，但编译阶段显示 `Nothing to compile`
- 处理: 为避免旧 class 被重复打包，补执行全量重编译
- [19:46:10] 已执行 `mvn -DskipTests clean package`
- 结果: `BUILD SUCCESS`
- 构建产物: `G:\zhiximini\backend-api\target\backend-1.0.0.jar`
- 发布副本: `G:\zhiximini\_deploy\backend-address-schema-fix-2026-04-21-194411.jar`

## 六、发布执行记录

- [19:47-19:48] 首次发布脚本因本地 PowerShell 变量名冲突中断
- 影响: 未替换服务器文件，线上版本未受影响
- [19:48-19:49] 重新执行发布
- 服务器动作:
  - 上传新包到 `/home/ubuntu/apps/backend-api/app.jar.new`
  - 备份线上旧包到 `/home/ubuntu/apps/backend-api/backups/app-before-address-schema-fix-2026-04-21-194411.jar`
  - 使用新包替换 `/home/ubuntu/apps/backend-api/app.jar`
  - 重启 `zhixi-backend.service`
  - 等待健康检查通过
- 健康检查结果:
  - `{"success":true,"message":"OK","data":{"status":"UP"}}`

## 七、线上验证结果

- 数据库表结构验证通过，`user_addresses` 关键字段已变为:
  - `recipient_name`
  - `recipient_phone`
  - `detail_address`
  - `is_default`
- 线上启动日志确认迁移已执行:
  - `Detected legacy column user_addresses.receiver_name -> recipient_name, applying rename migration.`
  - `Rename migration for user_addresses.receiver_name -> recipient_name completed.`
  - `Detected legacy column user_addresses.receiver_mobile -> recipient_phone, applying rename migration.`
  - `Rename migration for user_addresses.receiver_mobile -> recipient_phone completed.`
- 未登录接口验证:
  - 请求: `GET https://api.mashishi.com/api/addresses`
  - 结果: HTTP 400
  - 返回消息: `请先登录`
- 已登录接口验证:
  - 使用有效 Bearer token 请求 `GET https://api.mashishi.com/api/addresses`
  - 结果: HTTP 200
  - 返回体: `{"success":true,"message":"OK","data":[]}`

## 八、回退预案与结果

- 本次发布成功，未触发回退
- 如需回退，可使用服务器备份包:
  - `/home/ubuntu/apps/backend-api/backups/app-before-address-schema-fix-2026-04-21-194411.jar`

## 九、结论

- 地址页 500 的直接根因已修复
- 线上后端已兼容旧地址表字段名
- 当前小程序地址列表接口已恢复可用
