# 2026-04-30 规则页返现展示发布原子化说明

## 目标

1. 修复规则页邀请返现仍显示旧口径的问题
2. 保持当前发布面最小化
3. 不把无关前端改动混入本次上线

## 原子步骤

### A. 发布前备份

- 创建本地备份目录
- 同步当前工作区快照到 `G:\store`
- 同步同一份快照到 `beifenstore.git`

### B. 根因确认

- 核对规则页模板和默认文案
- 对比线上 `/api/cashbacks/rules`
- 确认问题来自后端规则接口旧版本

### C. 最小发布

- 仅发布 `backend-api`
- 不发布小程序代码包
- 不发布官网或后台

### D. 健康检查

- 检查 `systemctl` 运行状态
- 检查 `127.0.0.1:8080/api/health`
- 检查线上 `https://api.mashishi.com/api/cashbacks/rules`

### E. 回退依据

- 本地源码快照
- `beifenstore.git` 备份分支
- 服务器原 jar 备份
