# 2026-04-30 首页规则/权益悬浮按钮原子化说明
## 目标

1. 统一首页“规则 / 权益”入口位置。
2. 让两个入口处于同一条水平轴线上。
3. 增加持续可见的悬浮感，减少用户来回查找入口的成本。

## 原子步骤

### A. 双备份

1. 创建本地备份目录：
   - `G:\store\backups\20260430-212123-home-floating-actions`
2. 生成并保存本次操作文档：
   - 日志文档
   - 原子化说明文档
3. 使用 `robocopy` 将当前工作区完整快照同步到本地备份目录的 `code\zhiximini`。
4. 将同一份快照同步到远端备份仓工作区。
5. 在 `beifenstore.git` 中创建独立备份分支并推送。

### B. 首页结构调整

1. 删除首页原先分散在两个标题右侧的入口按钮。
2. 在“优选推荐”标题下方增加统一悬浮按钮组。
3. 保留“知禧会员权益”和“活动规则”锚点结构，确保滚动跳转目标不变。

### C. 首页样式调整

1. 新增统一悬浮按钮容器 `floating-action-rail`。
2. 新增按钮样式 `floating-action-btn` 及颜色变体。
3. 使用圆角、半透明背景、阴影、按压反馈形成悬浮效果。
4. 对窄屏和宽屏补充适配，避免按钮组遮挡内容或偏移异常。

### D. 交互复用校验

1. 确认 `scrollToBenefits()` 仍存在。
2. 确认 `scrollToRules()` 仍存在。
3. 确认 `ruleItems`、`visibleProducts`、`hasProductGrid` 仍与模板匹配。

## 本次修改落点

### 文件

1. `G:\zhiximini\wechat-app\pages\index\index.wxml`
2. `G:\zhiximini\wechat-app\pages\index\index.wxss`

### 结果

1. “权益”“规则”已从分散按钮改为同组悬浮入口。
2. 两个按钮位于同一 X 轴。
3. 点击后继续分别滚动到权益区和规则区。
4. 原商品区、权益区、规则区主体内容未被删除。

## 校验结论

1. 已通过 `git diff` 核对本次改动内容。
2. 已通过 `git status` 确认仅目标文件处于修改状态。
3. 已检查首页 JS 方法和数据字段，与新模板结构匹配。
4. 未进行微信开发者工具模拟器或真机可视化预览。

## 回退锚点

1. 本地回退快照：
   - `G:\store\backups\20260430-212123-home-floating-actions`
2. 远端回退分支：
   - `backup/20260430-212123-home-floating-actions`
3. 远端备份提交：
   - `6f4d6a2`
