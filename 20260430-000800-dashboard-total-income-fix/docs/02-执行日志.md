# 20260430-000800-dashboard-total-income-fix 执行日志

## 基本信息

- 任务编号：`20260430-000800-dashboard-total-income-fix`
- 执行时间：`2026-04-30 Asia/Shanghai`
- 工作目录：`G:\\zhiximini`
- 任务内容：修复管理后台看板“总收入”统计不真实问题

## 预操作状态

- `backend-api`
  - Git 提交：`de628b8`
  - 工作区状态：干净
- `zhixi-website`
  - Git 提交：`db816e4`
  - 工作区状态：存在未跟踪目录 `frontend-dist-upload/`

## 步骤记录

### 1. 建立任务目录

- 已创建本地目录：`G:\\store\\20260430-000800-dashboard-total-income-fix`
- 已创建远端备份工作目录：`G:\\store\\beifenstore-working\\20260430-000800-dashboard-total-income-fix`
- 已创建子目录：
  - `code`
  - `docs`

### 2. 初始问题分析结论

- 前端看板的“总收入”展示来自 `/api/admin/dashboard.totalIncome`
- 后端当前统计逻辑仅汇总 `order_status IN ('PAID','SHIPPED','COMPLETED')` 的 `total_amount`
- 高风险点：历史订单字段 `status -> order_status` 的迁移条件过窄，疑似导致已支付历史订单仍保留为 `PENDING`

### 3. 待执行

- 本地源码备份
- `beifenstore` 远端备份并推送
- 代码修复
- 本地验证
- 变更归档

### 4. 本地备份完成

- 已备份源码目录：
  - `G:\\store\\20260430-000800-dashboard-total-income-fix\\code\\backend-api`
  - `G:\\store\\20260430-000800-dashboard-total-income-fix\\code\\zhixi-website`
- 备份文件计数：
  - `backend-api`：`634`
  - `zhixi-website`：`766`
- 备份说明：
  - `backend-api` 备份时排除了构建产物目录 `target`
  - `zhixi-website` 备份时排除了大体积依赖/构建目录 `node_modules`、`dist`、`.package`

### 5. 远端备份工作区准备完成

- 已同步到工作区目录：
  - `G:\\store\\beifenstore-working\\20260430-000800-dashboard-total-income-fix`
- `beifenstore-working` 当前 Git 状态：
  - `?? 20260430-000800-dashboard-total-income-fix/`

### 6. 远端备份推送完成

- 远端备份仓库：`git@github.com:zhuzhustar0371/beifenstore.git`
- 首次备份提交：`ad1ee9e`
- 修正嵌套 Git 后的有效完整快照提交：`2cfb2b5`
- 说明：
  - 首次推送时源码目录包含 `.git`，被 Git 识别为嵌套仓库引用
  - 随后已在远端备份工作区移除嵌套 `.git` 元数据并重新提交
  - 当前 `2cfb2b5` 为可直接恢复的完整源码快照提交

### 7. 代码修复

- 修改仓库：`backend-api`
- 修改文件：
  - `src/main/java/com/zhixi/backend/config/DatabaseMigrationRunner.java`
  - `src/main/java/com/zhixi/backend/mapper/OrderMapper.java`
- 修复内容：
  - 放宽历史订单 `status -> order_status` 回填条件
  - 当 `order_status='PENDING'` 但旧 `status` 已是 `PAID/SHIPPED/COMPLETED/REFUNDED` 时，启动迁移自动纠正
  - `pay_amount` 回填从“仅 NULL”改为“NULL 或 0”时都补齐
  - 看板总收入与今日收入统计 SQL 增加对历史旧状态的兼容兜底
  - 收入汇总改为优先使用 `pay_amount`，为空时回退 `total_amount`

### 8. 本地验证

- 执行命令：`mvn -q -DskipTests package`
- 执行结果：成功
- 备注：
  - Maven 输出包含 JDK 兼容性 warning
  - 未出现编译失败
  - 本次未连接线上数据库，未直接执行生产数据修复验证

## 变更文件

- `backend-api/src/main/java/com/zhixi/backend/config/DatabaseMigrationRunner.java`
- `backend-api/src/main/java/com/zhixi/backend/mapper/OrderMapper.java`

## 验证结果

- 后端编译通过
- 历史订单字段同步逻辑已增强
- 看板收入 SQL 已具备历史脏状态兼容能力
- 仍需在目标环境启动应用后验证真实看板数值

## 回退记录

- 当前无
