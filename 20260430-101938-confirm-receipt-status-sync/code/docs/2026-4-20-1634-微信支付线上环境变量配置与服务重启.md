# 微信支付线上环境变量配置与服务重启

## 记录时间

- 时间：2026-04-20 16:34（Asia/Shanghai）
- 操作人：Codex
- 目标：将用户提供的微信支付商户参数写入线上后端服务环境变量，确认商户私钥与证书匹配，并完成服务重启与可用性验证。

## 本次输入信息

用户本次提供了以下关键信息：

- 商户证书序列号：`43556111DFA04364EEF593A44815B6A556F2BDDA`
- 商户证书内容：已提供 PEM 文本
- APIv3 密钥：已提供

本次未提供：

- 小程序 `AppSecret`

## 本次执行前的判断

在本次真正写入线上之前，已经提前确认：

- 线上服务为 `zhixi-backend.service`
- 环境变量来源为 `/home/ubuntu/apps/backend-api/.env`
- 线上已存在商户私钥文件：
  - `/home/ubuntu/apps/backend-api/cert/apiclient_key.pem`
- 小程序 `appid` 在项目中为：
  - `wx036abe08723e1e24`
- 当前支付链路为“小程序普通商户模式”

## 本次实际执行内容

### 1. 校验服务器私钥与用户提供的商户证书是否匹配

执行思路：

- 从服务器现有私钥 `apiclient_key.pem` 导出公钥并计算摘要
- 从用户提供的商户证书导出公钥并计算摘要
- 比对两者摘要是否一致

校验结果：

- 服务器私钥导出的公钥摘要：`e2df4e3fa5f1a7b3ff96c39c4613c6eaf2564d9623adcbc0382b36ea8381ac88`
- 商户证书导出的公钥摘要：`e2df4e3fa5f1a7b3ff96c39c4613c6eaf2564d9623adcbc0382b36ea8381ac88`

结论：

- 两者完全一致
- 服务器现有 `/home/ubuntu/apps/backend-api/cert/apiclient_key.pem` 可以继续使用
- 本次无需替换商户私钥文件

### 2. 创建并写入线上微信支付环境变量文件

写入位置：

- `/home/ubuntu/apps/backend-api/.env`

写入项：

- `WECHAT_MINIAPP_APP_ID=wx036abe08723e1e24`
- `WECHAT_MINIAPP_APP_SECRET=`
- `WECHAT_PAY_MCHID=1107238481`
- `WECHAT_PAY_APPID=wx036abe08723e1e24`
- `WECHAT_PAY_API_V3_KEY=已写入`
- `WECHAT_PAY_SERIAL_NUMBER=43556111DFA04364EEF593A44815B6A556F2BDDA`
- `WECHAT_PAY_PRIVATE_KEY_PATH=/home/ubuntu/apps/backend-api/cert/apiclient_key.pem`
- `WECHAT_PAY_NOTIFY_URL=https://api.mashishi.com/api/pay/wechat/notify`

说明：

- `WECHAT_PAY_MCHID=1107238481` 来自用户提供证书内容的主体信息
- `WECHAT_PAY_APPID` 与 `WECHAT_MINIAPP_APP_ID` 使用项目当前小程序 `appid`
- `WECHAT_MINIAPP_APP_SECRET` 由于用户未提供，本次保留为空值占位

### 3. 修正第一次写入失败的问题

本次过程里出现过一次 shell here-doc 转义问题：

- 第一次尝试直接通过远程 here-doc 写入 `.env`
- 因为转义不正确，文件末尾误混入了命令文本
- 我没有忽略这个异常，而是立即复核线上文件内容
- 随后改用“本地临时文件 + `scp` 上传 + 远端原子替换”的方式重新覆盖 `.env`

最终结果：

- 线上 `.env` 已经被纠正为干净内容
- 当前文件内容已复核通过

### 4. 重启后端服务

执行：

- `sudo systemctl restart zhixi-backend.service`

结果：

- 服务状态：`active (running)`
- 当前主进程 PID：`1599187`

补充说明：

- 重启后第一次本机端口探活出现短暂失败
- 后续查看 `app-systemd.log` 可确认应用于 `2026-04-20 16:33:48` 完成启动
- 属于重启窗口内的短暂未就绪，不是持续故障

### 5. 健康检查与监听检查

检查结果：

- 本机健康检查：`http://127.0.0.1:8080/api/health` 返回成功
- 外部健康检查：`https://api.mashishi.com/api/health` 返回 `HTTP 200`
- 端口监听：Java 进程已监听 `*:8080`

结论：

- 后端服务重启完成
- Nginx 反向代理正常
- 微信支付配置写入后未导致服务启动失败

## 本次未完成项

当前仍有一个明确缺口：

- `WECHAT_MINIAPP_APP_SECRET` 仍为空

影响：

- 小程序登录接口 `jscode2session` 仍无法正常换取 `openid`
- 没有新的 `openid`，就无法对“新登录的小程序用户”完整验证支付下单链路
- 已写入的微信支付商户参数本身已经就位，但端到端“小程序登录 -> 获取 openid -> 发起支付”流程仍缺最后一个登录密钥

## 当前系统状态

当前线上状态可以分成两部分理解：

### 已完成

- 商户私钥与商户证书匹配已确认
- 微信支付商户参数已写入线上 `.env`
- 商户证书序列号已写入
- APIv3 密钥已写入
- 回调地址已写入
- 后端服务已完成重启并健康

### 仍待补充

- 小程序 `AppSecret`

## 下一步动作建议

用户下一条只需要继续提供：

- 小程序 `AppSecret`

收到后应继续执行：

1. 更新 `/home/ubuntu/apps/backend-api/.env` 中的 `WECHAT_MINIAPP_APP_SECRET`
2. 重启 `zhixi-backend.service`
3. 验证小程序登录接口可成功换取 `openid`
4. 再验证小程序支付下单接口

## 涉及文件与环境

本次直接涉及：

- 服务器文件：`/home/ubuntu/apps/backend-api/.env`
- 服务器文件：`/home/ubuntu/apps/backend-api/cert/apiclient_key.pem`
- 服务器服务：`zhixi-backend.service`
- 本地日志：`g:\zhiximini\docs\2026-4-20-1634-微信支付线上环境变量配置与服务重启.md`

## 最终结论

本次已经把微信支付“商户侧配置”真正写上了线上环境，并完成了服务重启验证。当前唯一还缺的是小程序 `AppSecret`，补上它之后，就可以继续做小程序登录与真实支付的端到端验证。
