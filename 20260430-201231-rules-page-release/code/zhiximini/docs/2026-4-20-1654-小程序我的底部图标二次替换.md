# 小程序“我的”底部图标二次替换记录

- 记录时间：2026-04-20 16:52:41 +08:00
- 操作人：Codex
- 目标：
  - 将小程序底部导航“我的”图标，从上一版姜饼人风格图标，替换为用户最新提供的“小人”轮廓图
- 用户提供的源文件：
  - `C:\Users\朱鑫\Downloads\小人.png`

## 背景说明

- 本次变更不是首次替换“我的”图标
- 在上一轮处理中，`我的` 图标已经被替换成了一版姜饼人风格图标
- 用户随后明确要求再替换一次，并指定最新图片：
  - `C:\Users\朱鑫\Downloads\小人.png`
- 因此本次以最新用户图片为准，覆盖上一版“我的”图标结果

## 源图特征判断

- 源文件尺寸：
  - 宽 `200`
  - 高 `200`
- 可见像素区域：
  - `8,0 -> 191,199`
- 源图主体为透明背景上的“小人/用户”轮廓图标
- 图形结构清晰，适合作为小程序底部“我的”导航图标

## 本次处理方案

- 不改 `wechat-app/app.json`
  - 原因：原生 `tabBar` 的图标文件名引用已经正确
  - 只要替换同名文件，运行时就会直接读取新图标
- 将用户提供的最新原图保留到项目中作为参考源
  - 更新文件：`wechat-app/images/tab-user-reference.png`
- 从原图中提取可见 alpha 区域，去掉多余透明边缘
- 将主体图形按比例缩放后居中放入 `81x81` 透明画布
- 覆盖生成两套正式资源：
  - `wechat-app/images/tab-user.png`
  - `wechat-app/images/tab-user-active.png`

## 视觉处理说明

- 未选中态 `tab-user.png`
  - 使用灰色轮廓版本
  - 目的是与 tabBar 未选中文字颜色协调
- 选中态 `tab-user-active.png`
  - 使用深色轮廓版本
  - 目的是让当前选中项在白色底部栏上保持清晰可识别
- 输出格式使用透明背景 PNG
  - 原因：兼容小程序原生 `tabBar`
  - 原因：避免出现底色块

## 实际变更文件

- 更新参考图：
  - `g:\zhiximini\wechat-app\images\tab-user-reference.png`
- 覆盖正式图标：
  - `g:\zhiximini\wechat-app\images\tab-user.png`
  - `g:\zhiximini\wechat-app\images\tab-user-active.png`
- 更新日志：
  - `g:\zhiximini\OPS-CHANGELOG.md`
  - `g:\zhiximini\docs\2026-4-20-1654-小程序我的底部图标二次替换.md`

## 校验结果

- 文件输出校验：
  - `tab-user.png`：`1293` bytes
  - `tab-user-active.png`：`1293` bytes
  - `tab-user-reference.png`：`9253` bytes
- 生成尺寸校验：
  - `tab-user.png`：`81x81`
  - `tab-user-active.png`：`81x81`
- 可视校验：
  - 生成了临时白底预览图检查轮廓
  - 预览中可清晰识别为“小人/用户”图标
  - 预览确认后已删除临时预览文件，不保留在正式资源目录中
- 配置影响校验：
  - `wechat-app/app.json` 中“我的”底部栏仍然指向：
    - `images/tab-user.png`
    - `images/tab-user-active.png`
  - 因为文件名没有变化，所以无需额外改动页面或配置逻辑

## 结果预期

- 小程序底部“我的”图标将不再显示上一版姜饼人风格图标
- 未选中时，会显示灰色小人轮廓图标
- 选中时，会显示深色小人轮廓图标

## 备注

- 本次实现以用户最新提供图片为准，已经覆盖上一版“我的”图标结果
- 如果后续你想把“我的”选中态改成绿色、品牌色，或者想让线条更粗一些，我可以继续在当前这版上直接再调
