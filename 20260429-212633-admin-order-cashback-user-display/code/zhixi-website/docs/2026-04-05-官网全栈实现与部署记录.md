# 2026-04-05 官网全栈实现与部署记录

## 需求描述

基于 `docs/01-完整需求文档.md`，在 `zhixi-website` 内实现完整官网代码，并完成前后端打通、本地构建、服务器部署脚本、SSL 证书部署与 HTTPS 访问方案。

## 设计方案

- 前端：Vite + Vue3 + Vue Router
- 后端：Spring Boot + MyBatis + H2（本地默认，可切换 MySQL）
- 反向代理：Nginx（`mashishi.com`/`www.mashishi.com`/`admin.mashishi.com`/`api.mashishi.com`）
- 发布策略：先本地构建，发布前自动备份服务器当前版本，再解压新版本

## 代码实现清单

### 后端

- 创建后端工程：`backend/pom.xml`
- 业务表与初始化数据：`backend/src/main/resources/schema.sql`、`data.sql`
- 核心模块：
  - 用户注册与邀请码绑定
  - 商品查询
  - 订单创建与支付
  - 返现计算（个人返现 + 邀请批次返现）
  - 管理看板与流水查询

### 前端

- 创建前端工程：`frontend/package.json`、`vite.config.js`
- 页面：
  - 首页（注册、商品、下单、支付）
  - 返现规则页
  - 用户中心（订单/邀请/返现）
  - 管理页（看板/订单/返现）

### 运维与部署

- Nginx 配置：`deploy/nginx/mashishi.conf`
- systemd 配置：`deploy/systemd/zhixi-backend.service`
- 脚本：
  - `scripts/build_local.sh`
  - `scripts/deploy_to_server.sh`
  - `scripts/deploy_nginx_conf.sh`
  - `scripts/install_ssl.sh`
  - `scripts/install_systemd_service.sh`
  - `scripts/rollback.sh`
  - `scripts/health_check.sh`

## 执行命令记录

> 以下为关键命令记录（省略输出细节）

- 后端构建：`mvn -DskipTests package`
- 前端安装与构建：`npm install && npm run build`
- 接口冒烟：
  - `curl http://127.0.0.1:8080/api/health`
  - `curl -X POST http://127.0.0.1:8080/api/users/register ...`
  - `curl http://127.0.0.1:8080/api/products`

## 验证结果

- 后端打包成功
- 前端打包成功
- 健康接口与核心基础接口可访问
- 线上验证：
  - `https://mashishi.com` 返回 `HTTP/2 200`
  - `https://admin.mashishi.com` 返回 `HTTP/2 200`
  - `https://api.mashishi.com/api/health` 返回 `{"status":"UP"}`

## 部署参数占位（上线前填写）

- 服务器：`43.139.76.37`
- SSH 用户：`ubuntu`
- 部署目录：`/opt/zhixi`（可改）
- 证书目录：`/Users/caokun/Projects/github.com/zhixijiankang/SSL-CERTS`
- 证书文件：`<CERT_FILE>`、`<KEY_FILE>`

## 风险与后续建议

- 当前支付流程为“模拟支付”触发返现，未接微信支付正式链路。
- 当前鉴权为简化实现，生产建议加 JWT、RBAC、审计日志。
- 上线前请确保域名 DNS 已正确解析到服务器公网 IP。

## 部署问题与修复记录

1. 初次部署使用 `/opt/zhixi` 时遇到权限不足，改为 `/home/ubuntu/zhixi`。
2. 服务器未安装 Nginx，已执行安装：`sudo apt-get install -y nginx unzip`。
3. 首次站点访问 500，原因是 `/home/ubuntu` 目录无其他用户执行权限，已执行 `sudo chmod o+x /home/ubuntu` 并恢复访问。
4. SSL 证书为三个域名独立证书包，已分别部署到 `/etc/nginx/ssl` 并更新 Nginx 配置引用。

---

## UI 改版需求分析（基于截图反馈）

### 用户反馈

- 当前界面“太 Low”，希望立即提升为专业美观、简洁大方风格。
- 要求在不影响原有功能联调能力的前提下，完成视觉升级并上线。

### 设计目标

- 提升品牌质感：顶部导航、品牌标识、层级结构更清晰。
- 提升页面可读性：标题层次、模块分组、信息密度更合理。
- 提升交互体验：输入框、按钮、状态标签、空态展示统一。
- 保持开发效率：不改接口协议，不改业务逻辑，只做前端视觉重构。

## UI 设计与开发方案

- 全局样式重构：
  - 引入渐变背景、玻璃化卡片、统一圆角与阴影体系。
  - 增加响应式断点，移动端导航支持横向滚动。
- 导航与品牌区升级：
  - 深色渐变顶栏 + 品牌图形标记 + 辅助文案。
  - 胶囊式导航，激活态高亮更明显。
- 页面级重构：
  - 首页：Hero 强化、标签化卖点、表单与商品信息卡片化。
  - 规则页：规则卡片分栏、关键信息对齐展示。
  - 用户中心：分区统计、列表空态、状态标签。
  - 管理后台：数据看板强化、数值视觉权重提升。

## UI 改版操作记录

1. 修改文件：
   - `frontend/src/App.vue`
   - `frontend/src/styles.css`
   - `frontend/src/views/HomePage.vue`
   - `frontend/src/views/RulesPage.vue`
   - `frontend/src/views/UserCenterPage.vue`
   - `frontend/src/views/AdminPage.vue`
2. 本地验证：
   - 执行 `npm run build`，构建通过。
3. 线上发布：
   - 执行 `bash scripts/deploy_to_server.sh ubuntu 43.139.76.37 /home/ubuntu/zhixi`。
4. 线上验证：
   - `https://mashishi.com` 返回 `HTTP/2 200`。
   - `https://api.mashishi.com/api/health` 返回正常。

---

## 结构升级：官网与管理后台彻底分离

### 需求补充

- 官方网站与管理后台要分开部署。
- 官网页面不出现“管理后台”引导链接。
- 管理后台页面不出现“官网”引导链接。

### 实施方案

- 新增独立前端工程：`admin-frontend`（仅管理后台页面）。
- 既有 `frontend` 保留为官网前端，移除后台导航入口与后台路由。
- 调整 Nginx：
  - `mashishi.com` / `www.mashishi.com` -> `frontend/dist`
  - `admin.mashishi.com` -> `admin-frontend/dist`
- 更新构建与发布脚本，统一打包双前端产物。

### 修改清单

- 官网前端：
  - `frontend/src/App.vue`
  - `frontend/src/router.js`
- 新增管理后台独立工程：
  - `admin-frontend/*`
- 部署链路：
  - `scripts/build_local.sh`
  - `scripts/deploy_to_server.sh`
  - `deploy/nginx/mashishi.conf`

---

## 后端仓库彻底切换记录

### 目标

- 将后端 API 统一迁移到独立仓库 `backend-api`。
- `zhixi-website` 仅保留官网和管理后台前端代码。
- 删除 `zhixi-website/backend` 旧目录，避免双后端并存。

### 处理结果

- 已将后端代码迁移至：`/Users/caokun/Projects/github.com/zhixijiankang/backend-api`
- 已将服务器运行目录切换为：`/home/ubuntu/apps/backend-api`
- 已将 `zhixi-backend.service` 的 `ExecStart` 改为：
  - `/usr/bin/java -jar /home/ubuntu/apps/backend-api/app.jar`
- 已验证：
  - `systemctl is-active zhixi-backend.service` = `active`
  - `https://api.mashishi.com/api/health` 正常
- 已删除旧目录：
  - `/Users/caokun/Projects/github.com/zhixijiankang/zhixi-website/backend`

### 脚本改造

- `zhixi-website/scripts/build_local.sh`：仅构建两个前端
- `zhixi-website/scripts/deploy_to_server.sh`：仅发布前端，不再打包后端
- `zhixi-website/scripts/rollback.sh`：仅前端回滚
- `scripts/deploy_backend_api.sh`：增强为兼容 systemd 的独立后端部署脚本

---

## 重新部署与移动端适配记录

### 需求

- 重新部署 `zhixi-website` 到服务器。
- 重新部署 `manager-backend` 到服务器。
- 确保两个项目 UI 可在手机端良好访问。

### 本次实施

1. 初始化 `manager-backend` 独立前端工程（Vue + Vite）：
   - 新增 `package.json`、`index.html`、`vite.config.js`
   - 新增 `src/App.vue`、`src/api.js`、`src/main.js`、`src/styles.css`
   - 新增 `.env.example`
2. 新增独立部署脚本：
   - `scripts/deploy_manager_backend.sh`
3. 调整 Nginx：
   - `admin.mashishi.com` 根目录改为 `/home/ubuntu/apps/manager-backend/dist`
4. 执行部署：
   - 官网：`zhixi-website/scripts/deploy_to_server.sh`
   - 管理后台：`scripts/deploy_manager_backend.sh`
   - Nginx：`zhixi-website/scripts/deploy_nginx_conf.sh`

### 移动端适配说明

- 两个前端均包含 `<meta name="viewport" content="width=device-width, initial-scale=1.0" />`
- 后台样式提供 `@media (max-width: 900px)` 响应式规则：
  - 顶栏改为纵向布局
  - 统计卡片改为单列
  - 列表项改为纵向排布
  - 操作按钮改为可触控宽布局
- 官网已沿用响应式栅格与移动端导航策略，手机端可正常访问。

### 验证

- `https://mashishi.com` 返回 `HTTP/2 200`
- `https://admin.mashishi.com` 返回 `HTTP/2 200`
- `https://api.mashishi.com/api/health` 返回正常

---

## 服务器数据库配置记录（MySQL）

### 需求

- 安装并配置 MySQL，创建业务账号并允许远程访问。

### 执行结果

- 服务器已存在 MySQL 8 并处于运行状态。
- 监听地址已为 `0.0.0.0:3306`。
- 创建业务库：`zhixi`。
- 创建远程账号：`mysql-zhixi@'%'`。
- 认证插件：`mysql_native_password`。
- 授权：`GRANT ALL PRIVILEGES ON zhixi.*`。
- 远程访问验证：通过 TCP 连接测试成功。

### 安全说明

- 文档中不记录真实密码，统一使用占位符：`<DB_PASSWORD>`。
- 服务器维护账号密码未落盘到项目代码库。

---

## 后台登录与管理功能升级记录

### 需求

- 后台管理需要独立登录界面。
- `admins` 表新增管理员账号 `zhixi_admin`。
- 完成后台管理功能并保持专业 UI。

### 设计与实现

- 后端新增管理员鉴权体系：
  - 新增 `admins`、`admin_sessions` 支撑管理员登录与会话。
  - 新增接口：
    - `POST /api/admin/auth/login`
    - `POST /api/admin/auth/logout`
    - `GET /api/admin/auth/me`
  - 新增后台鉴权拦截器，对 `/api/admin/**` 执行 token 校验（鉴权接口除外）。
- 后台功能扩展：
  - 用户管理：查询用户列表。
  - 商品管理：查询商品、上下架切换。
  - 订单管理：查询订单、发货操作（填写物流单号）。
  - 邀请管理：查询邀请关系与首单状态。
  - 返现管理：查询返现流水。
- 前端新增专业登录页：
  - 登录后展示看板 + 五大管理模块。
  - 支持本地 token 持久化与自动恢复登录态。

### 数据库执行记录

- 在服务器 `43.139.76.37` 的 `zhixi` 库中执行：
  - 创建 `admins`（若不存在）
  - 创建 `admin_sessions`（若不存在）
  - 插入或更新 `zhixi_admin` 管理员账号

> 安全说明：文档不记录真实密码，统一使用占位符 `<ADMIN_PASSWORD>`。

### 验证

- 登录接口返回成功并签发 token。
- 带 token 访问 `/api/admin/dashboard` 成功。
- 不带 token 访问 `/api/admin/dashboard` 返回“请先登录后台”。

---

## 官网与后台重构（二期）

### 需求

- 官网视觉重构为“专业美观、大方简洁”。
- 官网新增登录能力：微信扫码登录、手机号验证码登录、微信扫码后绑定手机号。
- 注册/登录支持输入邀请码并建立邀请关系。
- 后台管理界面重构为更专业的信息架构与交互样式。
- 全站统一支持移动端访问。

### 设计方案

- 官网（`zhixi-website/frontend`）
  - 统一品牌头部，支持读取 `/logo.jpg`，并提供 `brand-mark.svg` 兜底。
  - 登录区拆分为：
    - 手机验证码登录（首次登录可带昵称+邀请码）
    - 微信扫码登录（扫码状态轮询 + 绑定手机号）
  - 用户中心改为“登录态驱动”，不再要求手工输入用户 ID。
  - 下单区改为自动使用当前登录用户，保证业务链路统一。
- 后台（`manager-backend`）
  - 登录页增加品牌化头部与安全提示。
  - 新增模块化导航（用户/商品/订单/邀请/返现），按模块切换查看，减少信息噪声。
  - 保持订单发货、商品上下架等核心操作不变。
- 后端（`backend-api`）
  - 新增用户鉴权与登录体系：
    - `user_sessions`
    - `sms_login_codes`
    - `user_wechat_auth`
    - `wechat_qr_sessions`
  - 新增接口：
    - `POST /api/auth/sms/send`
    - `POST /api/auth/sms/login`
    - `POST /api/auth/wechat/qr/create`
    - `GET /api/auth/wechat/qr/status/{scene}`
    - `POST /api/auth/wechat/mock-scan`（联调）
    - `POST /api/auth/wechat/bind-mobile`
    - `GET /api/auth/me`
    - `POST /api/auth/logout`
  - 新增邀请码绑定逻辑：首次注册或尚未绑定邀请关系的老用户，输入有效邀请码即可建关系。

### 代码改动清单（关键）

- `backend-api/src/main/resources/schema.sql`
  - 增加用户会话、短信验证码、微信绑定、微信扫码会话表。
- `backend-api/src/main/java/com/zhixi/backend/controller/AuthController.java`
- `backend-api/src/main/java/com/zhixi/backend/service/UserAuthService.java`
- `backend-api/src/main/java/com/zhixi/backend/model/*`（新增用户登录相关模型）
- `backend-api/src/main/java/com/zhixi/backend/mapper/*`（新增用户登录相关 Mapper）
- `backend-api/src/main/java/com/zhixi/backend/service/UserService.java`
  - 增加手机号创建用户与邀请码绑定能力。
- `zhixi-website/frontend/src/views/HomePage.vue`
  - 重构首页与登录区、下单区。
- `zhixi-website/frontend/src/views/UserCenterPage.vue`
  - 改为登录态驱动。
- `zhixi-website/frontend/src/api.js`
  - 新增认证 API 与 token 注入。
- `zhixi-website/frontend/src/App.vue`、`src/styles.css`
  - 重构品牌头部，支持 logo 兜底。
- `manager-backend/src/App.vue`、`src/styles.css`
  - 重构后台登录与模块导航交互。
- `scripts/create_zhixi_schema.sql`
  - 同步增加新增鉴权相关表结构。

### 安全与联调说明

- 文档中不记录真实密钥与密码，统一使用占位符：
  - `<DB_PASSWORD>`
  - `<REDIS_PASSWORD>`
  - `<WECHAT_WEB_APP_ID>`
  - `<WECHAT_WEB_APP_SECRET>`
- 当前短信验证码接口为联调模式，返回 `debugCode`，正式上线需接入真实短信网关并关闭返回验证码。
- 微信扫码登录已提供标准化扫码会话流程，联调阶段可使用 `mock-scan` 完成闭环验证。

### 线上兼容修复

- 发现线上 `users`/`invite_relations` 表字段采用 `mobile`、`inviter_user_id`、`invitee_user_id` 等命名。
- 后端已完成字段别名兼容与 SQL 修复，避免新登录接口在生产环境因历史表结构报错。
- 修复后已完成链路验证：
  - 短信发送 -> 验证码登录 -> `/api/auth/me`
  - 微信扫码会话 -> 绑定手机号 -> 登录成功
