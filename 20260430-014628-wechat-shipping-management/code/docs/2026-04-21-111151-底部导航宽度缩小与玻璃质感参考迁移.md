# 底部导航宽度缩小与玻璃质感参考迁移

- 操作时间：2026-04-21 11:11:51 +08:00
- 操作人：Codex
- 任务状态：已获批准，正在执行

## 1. 变更目标

- 缩小微信小程序底部导航栏的整体宽度。
- 将底部导航栏半透明效果向参考项目 `G:\bishe2 - 副本\admin\public\user-web` 中 `.footer-nav` 的玻璃胶囊风格靠拢。

## 2. 分析结论

- 当前底栏仍然偏宽，左右留白不足。
- 参考项目 `.footer-nav` 的关键特征是：
  - 居中悬浮
  - 宽度明显小于屏幕宽度
  - 半透明渐变背景
  - 清晰玻璃边缘
  - 更强的模糊与饱和度
  - 更干净的悬浮阴影
- 本次只需要调整 `wechat-app/custom-tab-bar/index.wxss`。

## 3. 批准记录

- 用户已明确批准本次修改。

## 4. 安全备份

- 本地独立备份目录：
  - `g:\zhiximini\_local_backups\2026-04-21-111151-tabbar-width-glass`
- 已备份文件：
  - `g:\zhiximini\wechat-app\custom-tab-bar\index.wxss`
- 备份副本：
  - `g:\zhiximini\_local_backups\2026-04-21-111151-tabbar-width-glass\index.wxss.bak`

## 5. 计划修改文件

- `g:\zhiximini\wechat-app\custom-tab-bar\index.wxss`

## 6. 计划修改内容

- 缩小 `.liquid-tabbar-shell` 的左右占位，提升左右留白。
- 调整 `.liquid-tabbar-shadow` 对应宽度，保持阴影与胶囊同步。
- 微调 `.liquid-tabbar` 的背景、边框、阴影与 `backdrop-filter`，靠近参考项目 `.footer-nav` 的风格。

## 7. 影响范围

- 仅影响底部导航栏宽度与视觉质感。
- 不影响页面逻辑、路由、接口和数据。

## 8. 回退方案

- 如效果不符合预期，直接使用以下备份文件恢复：
  - `g:\zhiximini\_local_backups\2026-04-21-111151-tabbar-width-glass\index.wxss.bak`

## 9. 执行结果

- 日志文件已创建。
- 样式文件已完成备份。
- 代码修改：已完成。

## 10. 实际修改内容

- `g:\zhiximini\wechat-app\custom-tab-bar\index.wxss`
  - `.liquid-tabbar-shell`
    - 左右边距从 `30rpx` 调整为 `72rpx`
    - 使底部导航整体宽度明显缩小
  - `.liquid-tabbar-shadow`
    - 左右范围改为 `10rpx`
    - 阴影模糊从 `14rpx` 提升到 `18rpx`
    - 阴影透明度从 `0.72` 提升到 `0.8`
  - `.liquid-tabbar`
    - 背景调整为更接近参考项目 `.footer-nav` 的统一半透明渐变：
      - `linear-gradient(180deg, rgba(255, 255, 255, 0.68) 0%, rgba(237, 244, 255, 0.62) 100%)`
    - 边框改为更轻的玻璃边缘：
      - `rgba(255, 255, 255, 0.42)`
    - 阴影改为更接近参考项目的悬浮投影：
      - `0 20rpx 44rpx rgba(117, 140, 175, 0.22)`
    - `backdrop-filter` 调整为：
      - `blur(30rpx) saturate(150%)`
  - `.liquid-tabbar-highlight`
    - 高光透明度略减，使参考样式那种统一玻璃面更自然

## 11. 调整逻辑说明

- 本次参考项目并不是直接照搬深色主题，而是提取其日间 `.footer-nav` 的关键玻璃参数：
  - 缩窄宽度
  - 半透明统一面
  - 细玻璃边框
  - 更强模糊
  - 更干净的悬浮阴影
- 小程序底栏结构、Tab 位置和交互逻辑保持不变。

## 12. 验证记录

- 本次实际仅修改 `index.wxss`。
- 未修改 `index.wxml`、`index.js`、跳转逻辑和业务功能。

## 13. 回退指引

- 如需恢复本次修改前版本，可直接使用以下备份文件覆盖：
  - `g:\zhiximini\_local_backups\2026-04-21-111151-tabbar-width-glass\index.wxss.bak`
