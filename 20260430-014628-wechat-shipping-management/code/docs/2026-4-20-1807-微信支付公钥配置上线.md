# 微信支付公钥配置上线

## 记录时间

- 时间：2026-04-20 18:07（Asia/Shanghai）
- 操作人：Codex
- 目标：使用用户提供的微信支付公钥文件，完成线上微信支付公钥模式配置，解决此前自动下载平台证书模式下的初始化风险。

## 本次输入

用户提供本地文件：

- `C:\Users\朱鑫\Downloads\pub_key.pem`

此前用户已提供微信支付公钥 ID：

- `PUB_KEY_ID_0111072384812026041400292210003804`

安全说明：

- 公钥不是私钥，安全等级低于商户私钥和 APIv3 密钥
- 本次仍未把公钥全文写入日志
- 本次未修改小程序前端
- 本次未把任何生产密钥写入仓库

## 本地公钥文件校验

校验文件：

- `C:\Users\朱鑫\Downloads\pub_key.pem`

校验结果：

- 首行：`-----BEGIN PUBLIC KEY-----`
- 末行：`-----END PUBLIC KEY-----`
- 行数：`9`
- SHA256：`7fcdb3f82af4014d27da281c248a73801fe43a2cf53d71c378a4d49bcae3f670`

结论：

- 文件格式是微信支付公钥 PEM 格式
- 不是商户证书
- 不是商户私钥
- 可用于后端 `RSAPublicKeyConfig`

## 线上写入位置

公钥写入路径：

- `/home/ubuntu/apps/backend-api/cert/wechatpay_public_key.pem`

环境变量文件：

- `/home/ubuntu/apps/backend-api/.env`

本次写入/更新的环境变量：

- `WECHAT_PAY_PUBLIC_KEY_ID=PUB_KEY_ID_0111072384812026041400292210003804`
- `WECHAT_PAY_PUBLIC_KEY_PATH=/home/ubuntu/apps/backend-api/cert/wechatpay_public_key.pem`

写入方式：

1. 上传本地 `pub_key.pem` 到服务器临时路径
2. 如果线上已有旧公钥，则先备份
3. 使用 `install -m 600` 写入正式公钥路径
4. 备份线上 `.env`
5. 合并更新公钥 ID 与公钥路径
6. 删除临时上传文件

## 线上校验

服务器公钥文件 SHA256：

- `7fcdb3f82af4014d27da281c248a73801fe43a2cf53d71c378a4d49bcae3f670`

结论：

- 服务器文件与用户本地 `pub_key.pem` 指纹一致
- 公钥上传没有发生内容变化

## 服务重启与健康检查

执行：

- `sudo systemctl restart zhixi-backend.service`

结果：

- 服务状态：`active`
- 本机健康检查：`http://127.0.0.1:8080/api/health` 成功
- 外部健康检查：`https://api.mashishi.com/api/health` 返回 HTTP 200

## 日志复核

此前旧日志中存在微信支付 SDK 初始化错误：

- `RESOURCE_NOT_EXISTS`
- `无可用的平台证书，请在商户平台-API安全申请使用微信支付公钥`

本次配置公钥并重启后，以新启动时间 `2026-04-20T18:06:45` 为界过滤日志：

- 发现启动成功日志
- 未发现新的 `RESOURCE_NOT_EXISTS`
- 未发现新的 `Failed to initialize WeChat Pay SDK`
- 未发现新的 `ERROR`

结论：

- 线上服务已进入配置了微信支付公钥的运行状态
- 旧的自动证书下载错误未在新启动后继续出现

## 当前微信支付配置状态

已配置：

- 小程序 AppID
- 小程序 AppSecret
- 商户号
- APIv3 密钥
- 商户证书序列号
- 商户私钥路径
- 支付回调地址
- 微信支付公钥 ID
- 微信支付公钥路径

已校验：

- 商户证书与服务器商户私钥匹配
- 微信支付公钥文件格式正确
- 微信支付公钥文件上传前后 SHA256 一致
- 后端服务重启后健康
- 外部 API 域名健康

## 下一步联调

当前已经具备发起小程序微信支付的关键配置。下一步应在微信开发者工具或真机中执行：

1. 小程序真实登录，确保后端拿到真实 `openid`
2. 创建真实订单
3. 点击支付，调用 `POST /api/orders/{orderId}/pay/wechat-miniapp`
4. 小程序收到支付参数后调用 `wx.requestPayment`
5. 确认是否弹起微信支付
6. 支付成功后验证 `https://api.mashishi.com/api/pay/wechat/notify` 回调
7. 核查订单状态是否由 `PENDING` 更新为已支付状态

## 本次结论

微信支付公钥模式已经配置上线。后端服务健康，公钥文件与本地文件一致，新启动日志中没有继续出现此前的自动证书下载错误。现在可以进入真实小程序支付联调。

