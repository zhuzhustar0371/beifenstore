# 底部导航两元素大小与垂直居中调整

- 操作时间：2026-04-21 09:32:56 +08:00
- 操作人：Codex
- 任务状态：已获批准，正在执行

## 1. 变更目标

- 调整微信小程序底部自定义导航栏中两个 `tab-item` 元素的视觉尺寸。
- 优化图标气泡、文字与胶囊容器之间的纵向对齐，使其垂直居中。
- 保持现有路由、切换逻辑、图标资源和业务功能不变。

## 2. 分析结论

- 本次问题定位在 `wechat-app/custom-tab-bar/index.wxss`。
- 现有底栏容器下内边距偏大，导致内容视觉重心略偏下。
- `tab-bubble` 尺寸与 `tab-label` 间距偏大，两个元素作为整体在胶囊中的垂直居中效果不够稳定。
- 本次优先采用最小改动方案，仅调整样式，不改结构和逻辑。

## 3. 批准记录

- 用户已明确批准修改。

## 4. 安全备份

- 本地独立备份目录：
  - `g:\zhiximini\_local_backups\2026-04-21-093256-tabbar-align`
- 已备份文件：
  - `g:\zhiximini\wechat-app\custom-tab-bar\index.wxss`
- 备份副本：
  - `g:\zhiximini\_local_backups\2026-04-21-093256-tabbar-align\index.wxss.bak`

## 5. 计划修改文件

- `g:\zhiximini\wechat-app\custom-tab-bar\index.wxss`

## 6. 计划修改内容

- 调整 `.liquid-tabbar` 的上下内边距和最小高度。
- 调整 `.tab-item` 的最小高度与内部间距。
- 缩小 `.tab-bubble` 和 `.tab-icon` 尺寸。
- 微调 `.tab-label` 的字号和行高。
- 轻微收敛激活态位移，避免视觉中心漂移。

## 7. 影响范围

- 仅影响微信小程序底部自定义导航栏的样式表现。
- 不影响页面跳转、登录、下单、接口、数据和发布逻辑。

## 8. 回退方案

- 如效果不符合预期，直接使用以下备份文件恢复：
  - `g:\zhiximini\_local_backups\2026-04-21-093256-tabbar-align\index.wxss.bak`

## 9. 执行结果

- 日志文件已创建。
- 样式文件已完成备份。
- 代码修改：已完成。

## 10. 实际修改内容

- `g:\zhiximini\wechat-app\custom-tab-bar\index.wxss`
  - `.liquid-tabbar`
    - `min-height` 从 `96rpx` 调整为 `88rpx`
    - `padding` 从 `12rpx 20rpx calc(10rpx + env(...))` 调整为 `10rpx 20rpx calc(8rpx + env(...))`
  - `.tab-item`
    - `gap` 从 `6rpx` 调整为 `4rpx`
    - `min-height` 从 `72rpx` 调整为 `64rpx`
  - `.tab-bubble`
    - 尺寸从 `68rpx * 68rpx` 调整为 `60rpx * 60rpx`
    - 圆角从 `34rpx` 调整为 `30rpx`
  - `.tab-bubble-active`
    - 激活态位移从 `translateY(-4rpx) scale(1.02)` 调整为 `translateY(-2rpx) scale(1.01)`
  - `.tab-icon`
    - 尺寸从 `34rpx * 34rpx` 调整为 `28rpx * 28rpx`
  - `.tab-label`
    - 字号从 `20rpx` 调整为 `18rpx`
    - 行高从 `1` 调整为 `1.1`

## 11. 修改效果

- 两个底部导航元素整体尺寸收敛，不再显得偏大偏重。
- 图标气泡与文字之间间距缩小，作为一个整体更接近胶囊中线。
- 激活态浮起效果保留，但不再把视觉中心明显拉偏。

## 12. 验证记录

- 已复核修改后的 `index.wxss` 内容，确认本次仅修改一个样式文件。
- 未执行构建、发布、推送；本次按用户请求只完成本地样式调整与留档。

## 13. 回退指引

- 如需恢复修改前版本，可直接覆盖：
  - 源文件：`g:\zhiximini\wechat-app\custom-tab-bar\index.wxss`
  - 备份文件：`g:\zhiximini\_local_backups\2026-04-21-093256-tabbar-align\index.wxss.bak`
