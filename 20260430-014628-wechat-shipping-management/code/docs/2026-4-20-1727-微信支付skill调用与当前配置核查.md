# 微信支付 skill 调用与当前配置核查

## 记录时间

- 时间：2026-04-20 17:27（Asia/Shanghai）
- 操作人：Codex
- 用户要求：配置微信支付，并调用微信支付相关 skill。

## 本次调用的 skill

- 已调用：`wechatpay-basic-payment`
- 适用原因：用户明确提出“微信支付配置”，属于微信支付基础支付接入场景。
- 本次按项目现状识别为：小程序支付、普通商户模式、后端生成支付参数、小程序端调用 `wx.requestPayment`。

## 本次核查范围

- `backend-api`
- `wechat-app`
- `docs`
- 项目根目录现有运维记录

## 关键发现

### 1. 当前代码已具备小程序微信支付主链路

后端已存在微信支付服务：

- `backend-api/src/main/java/com/zhixi/backend/service/WechatPayService.java`

该服务使用微信支付 API v3 Java SDK：

- `com.github.wechatpay-apiv3:wechatpay-java:0.2.14`

后端当前已经包含：

- JSAPI/小程序下单参数生成
- 微信支付 SDK 初始化
- 商户号、AppID、APIv3 Key、商户证书序列号、商户私钥路径、回调地址配置读取
- 退款服务初始化
- 商家转账服务初始化

小程序端已存在统一支付工具：

- `wechat-app/utils/pay.js`

小程序支付流程为：

1. 小程序请求后端接口：`POST /api/orders/{orderId}/pay/wechat-miniapp`
2. 后端返回 `timeStamp`、`nonceStr`、`package`、`signType`、`paySign`
3. 小程序调用 `wx.requestPayment`

### 2. 当前配置项已经落在 `application.yml`

后端配置文件：

- `backend-api/src/main/resources/application.yml`

当前微信支付相关配置项包括：

- `WECHAT_MINIAPP_APP_ID`
- `WECHAT_MINIAPP_APP_SECRET`
- `WECHAT_PAY_MCHID`
- `WECHAT_PAY_APPID`
- `WECHAT_PAY_API_V3_KEY`
- `WECHAT_PAY_SERIAL_NUMBER`
- `WECHAT_PAY_PRIVATE_KEY_PATH`
- `WECHAT_PAY_NOTIFY_URL`

当前默认回调地址：

- `https://api.mashishi.com/api/pay/wechat/notify`

当前默认小程序 AppID：

- `wx036abe08723e1e24`

当前默认商户号：

- `1107238481`

### 3. 历史日志显示线上曾完成过商户参数与 AppSecret 配置

已存在相关日志：

- `docs/2026-4-20-1616-微信支付配置准备与上线参数梳理.md`
- `docs/2026-4-20-1634-微信支付线上环境变量配置与服务重启.md`
- `docs/2026-4-20-1656-微信支付AppSecret配置与后端升级验证.md`

历史记录显示：

- 小程序 AppID 已确认
- 小程序 AppSecret 曾写入线上环境
- 微信支付商户号曾写入线上环境
- APIv3 密钥曾写入线上环境
- 商户证书序列号曾写入线上环境
- 商户私钥文件路径曾配置为 `/home/ubuntu/apps/backend-api/cert/apiclient_key.pem`
- 支付回调地址曾配置为 `https://api.mashishi.com/api/pay/wechat/notify`
- 线上后端曾替换为包含真实 `jscode2session` 逻辑的新 jar

### 4. 本次没有写入真实密钥

本次未新增、未展示、未写入任何真实商户密钥或小程序密钥。

原因：

- 微信支付 APIv3 Key、小程序 AppSecret、商户私钥都属于敏感信息
- 这些信息应只存在于服务器环境变量或服务器安全文件路径中
- 不应写入前端小程序代码
- 不应提交到仓库源码

## 本次执行验证

### 后端构建验证

执行位置：

- `g:\zhiximini\backend-api`

执行命令：

- `mvn -DskipTests package`

结果：

- 构建成功
- 生成 jar：
  - `backend-api/target/backend-1.0.0.jar`
- Maven 输出：`BUILD SUCCESS`

说明：

- 当前后端源码可以正常编译
- 微信支付 SDK 依赖可以正常解析
- `WechatPayService` 等支付相关代码未出现编译错误

### 小程序支付工具核查

核查文件：

- `wechat-app/utils/pay.js`

结论：

- 小程序端已使用后端真实支付接口
- 已封装 `wx.requestPayment`
- 已区分用户取消支付与微信支付异常

## 当前还需要真实联调确认的内容

当前代码与配置结构已经具备微信支付能力，但真实支付是否成功还取决于以下外部条件：

1. 微信商户平台中，小程序 AppID 与商户号绑定正确
2. 线上环境变量中的 APIv3 Key 与商户平台一致
3. 商户私钥文件与商户证书序列号匹配
4. 支付回调域名 `https://api.mashishi.com` 可公网访问，且 HTTPS 证书正常
5. 微信支付商户平台回调地址与后端配置一致
6. 用户在微信开发者工具或真机中完成真实 `wx.login()`，后端能拿到真实 `openid`
7. 使用真实订单发起一次小程序支付下单

## 本次结论

本次已按要求调用微信支付 skill，并完成项目当前微信支付配置核查。

当前项目不是“尚未接入微信支付”的状态，而是已经具备“小程序普通商户模式”支付链路。后端构建验证通过，下一步应做真机或微信开发者工具端到端联调：

1. 小程序真实登录
2. 创建真实订单
3. 发起微信支付
4. 验证支付回调
5. 验证订单状态变更

