# 线上 WECHAT_PAY_API_V3_KEY 配置日志

## 基本信息

- 操作时间：2026-04-23 20:43 CST
- 操作人：Codex
- 服务器：ubuntu@43.139.76.37
- 后端应用目录：/home/ubuntu/apps/backend-api
- 后端环境变量文件：/home/ubuntu/apps/backend-api/.env
- 后端服务：zhixi-backend.service
- 本次目标：设置线上微信支付 APIv3 密钥 `WECHAT_PAY_API_V3_KEY`，用于微信支付回调解密、接口验签相关能力。
- 敏感信息处理：日志不记录明文密钥，仅记录掩码 `eoHd****************aHjS`。

## 变更前分析

- 后端配置文件 `application.yml` 使用 `app.wechat.pay.api-v3-key: ${WECHAT_PAY_API_V3_KEY:}` 读取 APIv3 密钥。
- 后端服务 `zhixi-backend.service` 通过 `EnvironmentFile=-/home/ubuntu/apps/backend-api/.env` 加载线上环境变量。
- 线上 `.env` 已存在 `WECHAT_PAY_API_V3_KEY`，本次执行替换，不新增重复变量。
- 本次操作不涉及代码修改、构建、打包和前端发布，只进行线上配置变更与服务重启。

## 用户批准

- 用户提供 APIv3 密钥并明确批准：`批准写入线上 APIV3 密钥`

## 备份记录

- 备份对象：/home/ubuntu/apps/backend-api/.env
- 备份文件：/home/ubuntu/apps/backend-api/backups/.env-before-apiv3-key-20260423204334
- 备份方式：执行写入前使用 `cp -p` 保留原文件权限和时间属性。

## 执行步骤

1. 读取微信支付基础支付 skill，确认操作符合微信支付 APIV3 密钥配置场景。
2. 只读核对后端配置，确认变量名为 `WECHAT_PAY_API_V3_KEY`。
3. 只读核对 systemd 配置，确认服务加载 `/home/ubuntu/apps/backend-api/.env`。
4. 第一次执行时 PowerShell 本地转义远端 `bash` 命令失败，未完成远端写入。
5. 改用 SSH 标准输入传入远端脚本，避免本地 PowerShell 解释远端脚本语法。
6. 在远端创建或复用备份目录 `/home/ubuntu/apps/backend-api/backups`。
7. 备份原始 `.env` 到 `/home/ubuntu/apps/backend-api/backups/.env-before-apiv3-key-20260423204334`。
8. 替换 `.env` 中的 `WECHAT_PAY_API_V3_KEY`，并校验该变量只出现 1 次。
9. 重启后端服务：`sudo systemctl restart zhixi-backend.service`。
10. 检查服务状态：`active`。
11. 检查服务器内网健康接口：`http://127.0.0.1:8080/api/health` 返回 UP。
12. 检查公网健康接口：`https://api.mashishi.com/api/health` 返回 UP。
13. 检查管理后台首页：`https://admin.mashishi.com/` 返回 HTTP 200。

## 变更结果

- 当前线上 `WECHAT_PAY_API_V3_KEY`：已配置，掩码为 `eoHd****************aHjS`
- `.env` 中该变量数量：1
- 后端服务状态：`active`
- 内网健康检查：`{"success":true,"message":"OK","data":{"status":"UP"}}`
- 公网健康检查：`{"success":true,"message":"OK","data":{"status":"UP"}}`
- 管理后台检查：`HTTP/1.1 200 OK`
- 是否触发回退：否

## 变更文件

- 远端修改：/home/ubuntu/apps/backend-api/.env
- 远端备份：/home/ubuntu/apps/backend-api/backups/.env-before-apiv3-key-20260423204334
- 本地日志：G:\zhiximini\docs\2026-04-23-204334-线上APIV3密钥配置日志.md

## 回退方案

如配置后出现微信支付异常、回调解密异常或后端服务异常，可执行以下回退：

```bash
ssh ubuntu@43.139.76.37
cd /home/ubuntu/apps/backend-api
cp -p backups/.env-before-apiv3-key-20260423204334 .env
sudo systemctl restart zhixi-backend.service
sudo systemctl is-active zhixi-backend.service
curl -fsS http://127.0.0.1:8080/api/health
```

## 后续验证建议

- 在管理后台对返现失败单执行“新版重试/批准打款”。
- 如果后续微信支付返回签名、解密、回调相关错误，优先核对商户平台 APIv3 密钥是否与本次配置值一致。
- 如果仍返回“尚未获取该转账场景”，该问题与 APIv3 密钥无关，继续按转账场景权限排查。
