# 2026-04-22 微信扫码登录昵称头像共享再次恢复发布日志

## 1. 背景

本次工作是对“Web 端微信扫码登录与小程序微信授权登录共用昵称和头像”能力的再次恢复发布。

原因：

- 之前已经实现过一次。
- 之后由于本地误撤回，导致共享资料逻辑和前端头像展示被回退。
- 本次在重新分析并获得批准后，按“本地备份 -> 本地修改 -> 本地构建 -> 云端备份 -> 发布上线 -> 健康检查 -> 留档”的流程重新执行。

## 2. 开发前分析结论

撤回后存在以下问题：

- 后端 [UserAuthService.java](G:/zhiximini/backend-api/src/main/java/com/zhixi/backend/service/UserAuthService.java) 又退回成优先读取 `MINIAPP` 授权记录。
- Web 登录与小程序登录之间的昵称、头像没有共享同步。
- Web 前端顶部登录态头像显示被撤回。
- 用户中心头像显示被撤回，重新变成首字母占位。

确认不需要重写 Web 扫码登录流程本身，当前需要恢复的是：

- Web / MINIAPP 资料共享解析逻辑
- 登录后的资料同步逻辑
- 前端头像展示逻辑

## 3. 本地备份

本次修改前新增本地备份目录：

- `G:\zhiximini\_local_backups\20260422-165811-wechat-profile-share-restore`

备份文件：

- `UserAuthService.java`
- `App.vue`
- `UserCenterPage.vue`
- `styles.css`

## 4. 本地修改内容

### 4.1 后端恢复共享资料逻辑

修改文件：

- [UserAuthService.java](G:/zhiximini/backend-api/src/main/java/com/zhixi/backend/service/UserAuthService.java)
- [AuthController.java](G:/zhiximini/backend-api/src/main/java/com/zhixi/backend/controller/AuthController.java)

恢复内容：

1. `getUserAvatarUrl(...)` 改为统一从共享资料解析结果中读取头像。
2. 新增 `getSharedDisplayNickname(...)`，用于统一读取 Web / MINIAPP 共用昵称。
3. 新增共享资料辅助逻辑：
   - `resolveWechatProfile(...)`
   - `resolveSharedNickname(...)`
   - `resolveSharedAvatarUrl(...)`
   - `syncWechatProfile(...)`
   - `applyWechatProfileUpdate(...)`
   - `WechatProfileSnapshot`
4. 在以下场景增加资料同步：
   - 用户更新资料后
   - 小程序登录命中已存在用户后
   - Web OAuth 登录回调创建或命中用户后
5. `/api/auth/me` 和登录响应改为返回共享昵称，不再只偏向 MINIAPP 授权昵称。

### 4.2 前端恢复头像展示

修改文件：

- [App.vue](G:/zhiximini/zhixi-website/frontend/src/App.vue)
- [UserCenterPage.vue](G:/zhiximini/zhixi-website/frontend/src/views/UserCenterPage.vue)

恢复内容：

1. 顶部登录态重新显示头像：
   - 有 `avatarUrl` 时显示头像图片
   - 无头像时显示首字母占位
2. 用户中心重新显示头像：
   - 有 `avatarUrl` 时显示头像图片
   - 无头像时显示首字母占位

### 4.3 样式说明

本次未新增新的头像样式块，现有 [styles.css](G:/zhiximini/zhixi-website/frontend/src/styles.css) 中的头像样式仍然存在，恢复模板使用后即可重新生效。

## 5. 本地构建

### 5.1 后端构建

命令：

```bash
mvn -q -DskipTests package
```

结果：

- 构建成功
- 产物：`G:\zhiximini\backend-api\target\backend-1.0.0.jar`

### 5.2 前端构建

命令：

```bash
npm run build
```

结果：

- 构建成功
- 产物目录：`G:\zhiximini\zhixi-website\frontend\dist`

## 6. 云端备份

发布时间戳：

- `20260422170849`

发布前创建的线上备份：

- 后端备份：`/home/ubuntu/apps/backend-api/backups/app-20260422170849.jar`
- 前端备份：`/home/ubuntu/zhixi/backups/frontend-dist-20260422170849/`

## 7. 发布过程

### 7.1 后端发布

执行内容：

1. 备份当前线上 `/home/ubuntu/apps/backend-api/app.jar`
2. 上传本地新构建的 `backend-1.0.0.jar`
3. 覆盖线上 `app.jar`
4. 重启 `zhixi-backend.service`

### 7.2 前端发布

执行内容：

1. 备份当前线上 `/home/ubuntu/zhixi/current/frontend/dist`
2. 清空线上 `dist` 当前内容
3. 上传本地新的 `dist` 目录文件

## 8. 发布结果验证

### 8.1 服务状态

- `zhixi-backend.service` 状态：`active`

### 8.2 健康检查

- `https://api.mashishi.com/api/health` 返回：

```json
{"success":true,"message":"OK","data":{"status":"UP"}}
```

- `https://mashishi.com` 返回：`HTTP/1.1 200 OK`

### 8.3 备份校验

- 后端备份存在：`backend-backup-ok`
- 前端备份存在：`frontend-backup-ok`

## 9. 回滚方案

若本次恢复版本后续发现异常，可按以下步骤回滚：

1. 将 `/home/ubuntu/apps/backend-api/backups/app-20260422170849.jar` 复制回 `/home/ubuntu/apps/backend-api/app.jar`
2. 将 `/home/ubuntu/zhixi/backups/frontend-dist-20260422170849/` 中的内容恢复到 `/home/ubuntu/zhixi/current/frontend/dist/`
3. 重启 `zhixi-backend.service`
4. 再次检查：
   - `https://api.mashishi.com/api/health`
   - `https://mashishi.com`

## 10. 结论

本次“再次恢复实现”已经完成并上线：

- Web 端微信扫码登录与小程序微信授权登录重新共享昵称和头像
- Web 顶部登录态头像重新显示
- 用户中心头像重新显示
- 本地构建成功
- 云端备份成功
- 发布成功
- 健康检查通过

