# 管理后台返现枚举中文显示

## 记录时间

- 时间：2026-04-20 19:26（Asia/Shanghai）
- 操作人：Codex
- 目标：将管理后台返现流水中的英文枚举值改成中文展示。

## 用户反馈

用户截图中返现流水显示：

- 类型：`PERSONAL_ORDER`
- 状态：`PENDING`

用户要求：

- “把这个改成中文显示”

## 本次修改

修改文件：

- `zhixi-website/admin-frontend/src/App.vue`

新增中文映射：

### 返现类型

- `PERSONAL_ORDER` -> `自购返现`
- `INVITE_BATCH` -> `邀请返现`
- `DOWNLINE_FIRST_ORDER` -> `邀请首单奖励`
- `DOWNLINE_REPEAT_ORDER` -> `邀请复购奖励`

### 返现状态

- `PENDING` -> `待结算`
- `PROCESSING` -> `打款中`
- `TRANSFERRED` -> `已到账`
- `CANCELLED` -> `已取消`
- `FAILED` -> `打款失败`

说明：

- 后端接口仍保留原始枚举值
- 仅管理后台前端展示层转换为中文
- 操作逻辑仍根据原始状态判断，例如 `PENDING` 才能点击“批准打款”

## 构建与部署

执行目录：

- `g:\zhiximini\zhixi-website\admin-frontend`

构建命令：

- `npm run build`

结果：

- 构建成功
- 新 JS 资源：
  - `dist/assets/index-Bo5VVskl.js`
- CSS 资源：
  - `dist/assets/index-BeHjQguY.css`

部署位置：

- `/home/ubuntu/apps/manager-backend/dist`

部署步骤：

1. 打包本地 `dist`
2. 上传服务器临时目录
3. 备份旧线上 `dist`
4. 覆盖线上静态资源
5. 删除 `._*` 元数据文件

## 验证

公网首页：

- `https://admin.mashishi.com/`

验证结果：

- HTML 返回 200
- 新 JS 资源返回 200：
  - `/assets/index-Bo5VVskl.js`

源码核查：

- 已包含 `自购返现`
- 已包含 `待结算`
- 已包含 `打款中`
- 已包含 `已到账`

## 预期效果

刷新管理后台后，返现流水应显示：

- 类型：`自购返现`
- 状态：`待结算`

不再直接显示：

- `PERSONAL_ORDER`
- `PENDING`

## 注意事项

- 如果浏览器仍显示英文枚举，可能是旧 JS 缓存
- 可强制刷新，或无痕窗口打开 `https://admin.mashishi.com/`

