# 2026-04-22 微信扫码登录昵称头像共享恢复重发发布日志

## 一、背景

用户在确认 Web 端微信扫码登录与小程序微信授权登录共享昵称和头像后，误将本地实现回撤。

本次工作是在本地恢复备份后，重新执行一次完整上线流程，确保：

- 后端微信资料共享逻辑恢复
- Web 端头像展示恢复
- 小程序与 Web 端共享昵称/头像逻辑重新上线
- 线上版本可继续稳定访问

## 二、本地恢复来源

恢复前使用的本地备份目录：

- `G:\zhiximini\_local_backups\20260422_025724_wechat_profile_share`

恢复回来的文件：

- `G:\zhiximini\backend-api\src\main\java\com\zhixi\backend\service\UserAuthService.java`
- `G:\zhiximini\zhixi-website\frontend\src\App.vue`
- `G:\zhiximini\zhixi-website\frontend\src\views\UserCenterPage.vue`
- `G:\zhiximini\docs\2026-04-22-微信扫码登录回跳修复发布.md`

## 三、恢复后的核心内容

### 3.1 后端微信资料共享

`UserAuthService.java` 中恢复了以下逻辑：

- `resolveWechatProfile(...)`
- `resolveSharedNickname(...)`
- `resolveSharedAvatarUrl(...)`
- `syncWechatProfile(...)`
- `applyWechatProfileUpdate(...)`

这些逻辑用于在以下场景共享昵称和头像：

- Web 微信扫码登录
- 小程序微信授权登录
- 用户手动更新个人资料

同时恢复了以下同步点：

- 小程序已有用户登录后同步资料
- Web OAuth 回调后同步资料
- 个人资料更新后同步到 `WEB` / `MINIAPP` 两条授权记录

### 3.2 前端头像展示

恢复了以下前端展示改动：

- `App.vue` 顶部登录态显示头像
- `UserCenterPage.vue` 用户中心头像优先展示图片
- `styles.css` 补充头像容器裁切样式

效果是：

- 有 `avatarUrl` 时显示微信头像
- 没有头像时继续显示首字母占位

## 四、本地构建

### 4.1 后端构建

命令：

```bash
mvn -q -DskipTests package
```

结果：

- 构建成功
- 输出包：`G:\zhiximini\backend-api\target\backend-1.0.0.jar`

### 4.2 前端构建

命令：

```bash
npm run build
```

结果：

- 构建成功
- 输出目录：`G:\zhiximini\zhixi-website\frontend\dist`

## 五、线上发布

### 5.1 发布前备份

发布前先对当前线上稳定版本做了备份：

- 后端备份：`/home/ubuntu/apps/backend-api/backups/app-20260422164713.jar`
- 前端备份：`/home/ubuntu/zhixi/backups/frontend-dist-20260422164713/`

### 5.2 后端发布

执行过程：

1. 将线上 `app.jar` 复制到备份目录。
2. 用本地新构建的 `backend-1.0.0.jar` 覆盖线上 `app.jar`。
3. 重启 `zhixi-backend.service`。

### 5.3 前端发布

执行过程：

1. 将线上 `dist` 目录内容备份到新目录。
2. 清空线上 `dist` 当前内容。
3. 将本地构建产物 `G:\zhiximini\zhixi-website\frontend\dist` 覆盖上传到线上静态目录。

## 六、验证结果

### 6.1 服务状态

- `zhixi-backend.service` 状态为 `active`

### 6.2 公网验证

- `https://api.mashishi.com/api/health` 返回 `UP`
- `https://mashishi.com` 返回 `HTTP/1.1 200 OK`

## 七、回滚路径

如果后续再次出现问题，可按以下方式回滚：

1. 将 `/home/ubuntu/apps/backend-api/backups/app-20260422164713.jar` 恢复回 `/home/ubuntu/apps/backend-api/app.jar`
2. 将 `/home/ubuntu/zhixi/backups/frontend-dist-20260422164713/` 恢复回 `/home/ubuntu/zhixi/current/frontend/dist/`
3. 重启 `zhixi-backend.service`
4. 重新检查：
   - `https://api.mashishi.com/api/health`
   - `https://mashishi.com`

## 八、结论

本次本地恢复后的版本已经重新上线，微信扫码登录资料共享逻辑和前端头像展示都已恢复，线上健康检查通过。

