# 2026-04-23 00:12 本地退款接口测试记录

## 1. 测试目标

- 验证管理后台退款接口 `POST /api/admin/orders/{orderId}/refund` 在本地是否可正常执行。
- 验证退款前后订单状态字段是否按预期变化：
  - `order_status`: `PAID` -> `REFUNDED`
  - `refund_status`: `NONE` -> `SUCCESS`
  - `refund_no`: `MANUAL`
  - `refund_id`: `MANUAL`

## 2. 风险控制原则

- 本次没有直接连接生产数据库做退款测试。
- 原因：当前退款接口会改订单状态，且本地启动新版本后端还可能触发库结构迁移，直接连生产库存在误改线上数据风险。
- 本次采用本地隔离 H2 数据库进行接口测试，测试数据与线上隔离。

## 3. 本地测试环境

- 工作目录：`G:\zhiximini`
- 后端目录：`G:\zhiximini\backend-api`
- 临时测试目录：`G:\zhiximini\_tmp\local-refund-test`
- 临时数据库：`G:\zhiximini\_tmp\local-refund-test\db\refunddb`
- 本地后端日志：
  - `G:\zhiximini\_tmp\local-refund-test\backend.out.log`
  - `G:\zhiximini\_tmp\local-refund-test\backend.err.log`

## 4. 前置分析结果

### 4.1 远端环境只用于信息确认，不用于本次退款写操作

- 已确认远端库 `43.139.76.37:3306/zhixi` 可连接。
- 已确认线上存在多笔 `MOCK` 与 `WECHAT` 已支付订单。
- 已确认线上 `orders` 表当时缺少 `refund_no` 列。
- 因为该列缺失且退款测试会改订单状态，所以未直接在远端库执行退款。

### 4.2 本地后端运行产物问题

- 初始运行的 `target/backend-1.0.0.jar` 时间为 `2026-04-22 17:22:17`。
- `target/classes` 下最新编译类文件时间为 `2026-04-22 23:07:59` 之后。
- 结论：旧 jar 不是当前最新源码对应产物，不能用于验证本次退款改动。
- 处理：重新执行了 `mvn -q -DskipTests package`，生成新的运行产物。

## 5. 实际执行步骤

### 5.1 首次本地隔离启动

- 以 H2 本地文件库方式启动后端。
- JVM 受本机页文件限制影响，多次调整为低内存参数后成功启动：
  - `-Xint`
  - `-Xss256k`
  - `-Xms32m`
  - `-Xmx128m`
  - `-XX:MaxMetaspaceSize=128m`
  - `-XX:+UseSerialGC`
- 启动成功后，`GET http://127.0.0.1:8080/api/health` 返回：

```json
{"success":true,"message":"OK","data":{"status":"UP"}}
```

### 5.2 本地隔离库兼容性补齐

本地 H2 基于当前初始化脚本和历史兼容逻辑启动后，实际暴露出若干与当前代码不一致的结构问题。为完成本地退款接口测试，仅在临时 H2 库中做了兼容补齐：

- 新增 `orders.refund_no`
- 新增 `admin_roles.role_code`
- 新建 `admin_role_rel`
- 新增 `products.main_image_url`

说明：

- 这些调整只发生在 `G:\zhiximini\_tmp\local-refund-test\db\refunddb`。
- 没有修改项目源码文件。
- 没有修改生产数据库。

### 5.3 插入本地测试数据

插入了一条本地管理员会话和一笔本地测试订单：

- 管理员 token：`localrefundtoken202604230001`
- 测试订单号：`LOCALREFUND202604230001`
- 测试订单 ID：`2`
- 初始状态：
  - `status=PAID`
  - `order_status=PAID`
  - `pay_type=MOCK`
  - `refund_status=NONE`
  - `refund_no=NULL`
  - `refund_id=NULL`

插入后离线查询结果：

```text
ORDER_READY=2,LOCALREFUND202604230001,PAID,PAID,MOCK,NONE,null,null
```

### 5.4 重新打包并启动最新 jar

执行：

```powershell
mvn -q -DskipTests package
```

然后以同一套本地 H2 库重新启动最新 `backend-1.0.0.jar`，并关闭 SQL 初始化重复执行：

- `SQL_INIT_MODE=never`

重启后健康检查再次通过：

```json
{"success":true,"message":"OK","data":{"status":"UP"}}
```

### 5.5 发起本地后台退款接口测试

先使用本地管理员 token 调后台订单列表接口，再调退款接口：

```http
GET /api/admin/orders?page=1&size=5
Authorization: Bearer localrefundtoken202604230001
```

```http
POST /api/admin/orders/2/refund
Authorization: Bearer localrefundtoken202604230001
Content-Type: application/json

{"reason":"本地退款接口测试"}
```

退款接口实际返回：

```json
{"success":true,"message":"OK","data":null}
```

## 6. 测试结果

### 6.1 接口结果

- 管理后台订单查询接口可正常访问。
- 退款接口 `POST /api/admin/orders/2/refund` 返回成功。

### 6.2 数据结果

退款完成后，离线查询订单结果：

```text
ORDER_STATE=2,LOCALREFUND202604230001,PAID,REFUNDED,MOCK,SUCCESS,MANUAL,MANUAL,2026-04-23 00:11:47.548429
```

字段变化如下：

- `status`: 保持 `PAID`
- `order_status`: `PAID` -> `REFUNDED`
- `pay_type`: `MOCK`
- `refund_status`: `NONE` -> `SUCCESS`
- `refund_no`: `NULL` -> `MANUAL`
- `refund_id`: `NULL` -> `MANUAL`

说明：

- 对于 `MOCK` 支付订单，当前退款逻辑走人工退款分支。
- 本地测试结果与预期一致。

## 7. 过程中发现的问题

### 7.1 运行产物过期

- `target/backend-1.0.0.jar` 最初不是最新源码构建结果。
- 若直接用旧 jar 测试，会得到过期的 Mapper / SQL 行为，结论不可靠。

### 7.2 本地隔离库与当前代码存在兼容差异

本地 H2 隔离库在运行过程中暴露了这些兼容点：

- `orders.refund_no` 缺失
- `admin_role_rel` 缺失
- `admin_roles.role_code` 缺失
- `products.main_image_url` 缺失

这说明当前退款改动依赖的库结构较新，若后续要在其他本地环境或测试库复现，需要同步这些结构兼容项。

## 8. 清理动作

- 本地临时后端进程已停止。
- 停止时间点：本次测试完成后立即停止。
- 临时测试数据仍保留在：
  - `G:\zhiximini\_tmp\local-refund-test`

保留原因：

- 便于后续复查本次接口测试结果。
- 便于继续做退款回调、前端联调或二次验证。

## 9. 最终结论

- 本地最新后端代码在隔离环境下，退款接口已实际调用成功。
- `MOCK` 已支付订单能够被后台接口成功退款。
- 订单状态已按预期从 `PAID/NONE` 变为 `REFUNDED/SUCCESS`。
- 本次测试未修改生产数据库、未动线上订单、未影响线上服务。
