# 后端重启部署日志

- 时间: 2026-04-22 02:03 - 02:04 (Asia/Shanghai)
- 执行人: Codex
- 目标: 将本地最新代码打包并部署到远端 `zhixi-backend.service`，重启后端服务以验证头像上传修复

## 一、执行背景

前序已经完成头像上传链路修复，核心改动包括：

1. 统一上传目录解析
2. 头像上传改为流式复制落盘
3. 上传目录自动创建与回退
4. 静态资源映射与读取路径统一

用户要求：

- 重启后端服务
- 再试一次头像上传

由于当前小程序前端 `baseUrl` 指向 `https://api.mashishi.com`，本次操作采用远端部署方式，而不是仅重启本机进程。

## 二、远端环境确认

确认远端服务信息：

1. SSH 连通
   - 主机: `43.139.76.37`
   - 用户: `ubuntu`
2. systemd 服务存在
   - `zhixi-backend.service`
3. 服务配置
   - `ExecStart=/usr/bin/java -jar /home/ubuntu/apps/backend-api/app.jar`
   - `WorkingDirectory=/home/ubuntu/apps/backend-api`
4. 当前远端目录
   - `/home/ubuntu/apps/backend-api`

## 三、本地构建

执行命令：

1. `mvn -DskipTests package`

结果：

- 构建成功
- 产物生成于：
  - `G:\zhiximini\backend-api\target\backend-1.0.0.jar`

## 四、远端备份

在替换远端 `app.jar` 前，先执行备份：

1. 备份命令
   - `cp /home/ubuntu/apps/backend-api/app.jar /home/ubuntu/apps/backend-api/backups/app-20260422020314.jar`
2. 备份结果
   - 成功生成远端备份文件
   - 备份名: `app-20260422020314.jar`

## 五、远端部署

执行步骤：

1. 使用 `scp` 将本地新 jar 上传到远端
   - 本地: `G:\zhiximini\backend-api\target\backend-1.0.0.jar`
   - 远端: `/home/ubuntu/apps/backend-api/app.jar`
2. 使用 systemd 重启服务
   - `sudo systemctl restart zhixi-backend.service`

## 六、启动结果

重启后服务状态：

- `zhixi-backend.service` 为 `active (running)`
- 主进程 PID: `2070107`
- 监听端口: `8080`

应用启动日志确认：

1. Spring Boot 成功启动
2. 上传目录解析日志输出：
   - `Using upload directory: /home/ubuntu/apps/backend-api/uploads`
3. Tomcat 已监听 `8080`
4. 数据库连接正常

## 七、健康检查

执行命令：

- `curl -fsS http://127.0.0.1:8080/api/health`

返回结果：

```json
{"success":true,"message":"OK","data":{"status":"UP"}}
```

说明：

- 服务对外可用
- 后端已完成重启

## 八、回退方案

如果后续发现头像上传仍异常，可执行以下回退：

1. 将远端 `app.jar` 替换回备份文件
   - `/home/ubuntu/apps/backend-api/backups/app-20260422020314.jar`
2. 重启 `zhixi-backend.service`
3. 再次执行健康检查

## 九、后续验证建议

1. 在小程序开发者工具中重新执行头像上传
2. 如果仍失败，优先查看前端弹出的后端真实错误信息
3. 如需要，继续检查远端 `app-systemd.log` 中的上传错误栈
