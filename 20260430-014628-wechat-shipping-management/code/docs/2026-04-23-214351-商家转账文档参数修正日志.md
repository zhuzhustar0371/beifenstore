# 商家转账升级版文档参数修正日志

## 基本信息

- 操作时间：2026-04-23 21:43 CST
- 操作人：Codex
- 服务器：ubuntu@43.139.76.37
- 后端应用目录：/home/ubuntu/apps/backend-api
- 后端环境变量文件：/home/ubuntu/apps/backend-api/.env
- 后端服务：zhixi-backend.service
- 本次目标：按微信支付商家转账升级版文档修正现金营销场景相关线上环境变量。

## 变更前分析

- 当前系统已接入微信支付商家转账升级版接口：`/v3/fund-app/mch-transfer/transfer-bills`。
- 当前返现业务使用现金营销场景，线上已配置 `WECHAT_PAY_TRANSFER_SCENE_ID=1000`。
- 文档要求现金营销场景需要按转账背景传入报备信息：`活动名称`、`奖励说明`。当前代码已按这两个字段构造 `transfer_scene_report_infos`。
- 文档中现金营销的用户收款感知为 `活动奖励`，而代码默认值曾为 `现金奖励`，线上未显式配置 `WECHAT_PAY_TRANSFER_USER_RECV_PERCEPTION`。
- 本次采用线上环境变量覆盖方式修正，不修改代码、不重新构建。
- 风险说明：如果商户平台未实际开通场景 ID `1000`，仍会返回“尚未获取该转账场景”；该问题需要在微信支付商户平台产品中心开通或换成已开通场景 ID。

## 用户批准

- 用户明确批准：`批准按文档修正商家转账参数`

## 备份记录

- 备份对象：/home/ubuntu/apps/backend-api/.env
- 备份文件：/home/ubuntu/apps/backend-api/backups/.env-before-transfer-doc-params-20260423214351
- 备份方式：执行写入前使用 `cp -p` 保留原文件权限和时间属性。

## 写入配置

```env
WECHAT_PAY_TRANSFER_SCENE_ID=1000
WECHAT_PAY_TRANSFER_ACTIVITY_NAME=知禧返现
WECHAT_PAY_TRANSFER_REWARD_DESC=消费返现奖励
WECHAT_PAY_TRANSFER_USER_RECV_PERCEPTION=活动奖励
```

## 执行步骤

1. 读取微信支付基础支付 skill，确认任务属于商家转账升级版参数修正。
2. 使用 Base64 传递中文配置值，避免 PowerShell/SSH 编码或转义问题。
3. 备份线上 `.env` 到 `/home/ubuntu/apps/backend-api/backups/.env-before-transfer-doc-params-20260423214351`。
4. 在 `.env` 中插入或替换四个商家转账参数，并校验每个变量只出现 1 次。
5. 重启后端服务：`sudo systemctl restart zhixi-backend.service`。
6. 检查后端服务状态：`active`。
7. 检查服务器内网健康接口：`http://127.0.0.1:8080/api/health` 返回 UP。
8. 检查公网健康接口：`https://api.mashishi.com/api/health` 返回 UP。
9. 检查管理后台首页：`https://admin.mashishi.com/` 返回 HTTP 200。

## 验证结果

- 远端 `.env` 当前配置：
  - `WECHAT_PAY_TRANSFER_SCENE_ID=1000`
  - `WECHAT_PAY_TRANSFER_ACTIVITY_NAME=知禧返现`
  - `WECHAT_PAY_TRANSFER_REWARD_DESC=消费返现奖励`
  - `WECHAT_PAY_TRANSFER_USER_RECV_PERCEPTION=活动奖励`
- 后端服务状态：`active`
- 内网健康检查：`{"success":true,"message":"OK","data":{"status":"UP"}}`
- 公网健康检查：`{"success":true,"message":"OK","data":{"status":"UP"}}`
- 管理后台检查：`HTTP/1.1 200 OK`
- 是否触发回退：否

## 变更文件

- 远端修改：/home/ubuntu/apps/backend-api/.env
- 远端备份：/home/ubuntu/apps/backend-api/backups/.env-before-transfer-doc-params-20260423214351
- 本地日志：G:\zhiximini\docs\2026-04-23-214351-商家转账文档参数修正日志.md

## 回退方案

如配置后后端服务异常或需要恢复原参数，可执行：

```bash
ssh ubuntu@43.139.76.37
cd /home/ubuntu/apps/backend-api
cp -p backups/.env-before-transfer-doc-params-20260423214351 .env
sudo systemctl restart zhixi-backend.service
sudo systemctl is-active zhixi-backend.service
curl -fsS http://127.0.0.1:8080/api/health
```

## 后续验证建议

- 在管理端返现页对失败单执行“新版重试/批准打款”。
- 如果微信支付仍返回“你尚未获取该转账场景”，需要登录微信支付商户平台：产品中心 -> 商家转账 -> 产品设置，确认商户号已经获取场景 ID `1000`。
- 如果返回其他参数错误，优先检查微信接口错误中的字段名，并对照升级版发起转账接口文档修正对应环境变量或代码字段。
