# 底部导航栏整体纵向高度缩小

- 操作时间：2026-04-21 11:21:42 +08:00
- 操作人：Codex
- 任务状态：已获批准，正在执行

## 1. 变更目标

- 缩小底部导航栏整体纵向高度。
- 保持当前横向位置、玻璃质感和业务逻辑不变。

## 2. 分析结论

- 当前纵向尺寸主要由以下几层共同撑起：
  - `.liquid-tabbar`
  - `.tab-list`
  - `.tab-item`
  - `.tab-content`
  - `.tab-bubble`
- 若只改外层高度，内部内容会拥挤，因此需要联动缩小。

## 3. 批准记录

- 用户已明确批准本次缩小纵向高度。

## 4. 安全备份

- 本地独立备份目录：
  - `g:\zhiximini\_local_backups\2026-04-21-112142-tabbar-height-reduce`
- 已备份文件：
  - `g:\zhiximini\wechat-app\custom-tab-bar\index.wxss`
- 备份副本：
  - `g:\zhiximini\_local_backups\2026-04-21-112142-tabbar-height-reduce\index.wxss.bak`

## 5. 计划修改文件

- `g:\zhiximini\wechat-app\custom-tab-bar\index.wxss`

## 6. 计划修改内容

- 缩小 `.liquid-tabbar` 的 `min-height` 与上下 padding。
- 同步缩小 `.tab-list`、`.tab-item`、`.tab-content` 的 `min-height`。
- 视情况略微缩小 `.tab-bubble` 和图标尺寸。

## 7. 影响范围

- 仅影响底部导航栏整体纵向尺寸。
- 不影响路由、业务逻辑、接口和数据。

## 8. 回退方案

- 如效果不符合预期，直接使用以下备份文件恢复：
  - `g:\zhiximini\_local_backups\2026-04-21-112142-tabbar-height-reduce\index.wxss.bak`

## 9. 执行结果

- 日志文件已创建。
- 样式文件已完成备份。
- 代码修改：已完成。

## 10. 实际修改内容

- `g:\zhiximini\wechat-app\custom-tab-bar\index.wxss`
  - `.liquid-tabbar`
    - `min-height` 从 `96rpx` 调整为 `84rpx`
    - `padding` 从 `10rpx 24rpx calc(10rpx + env(...))` 调整为 `8rpx 24rpx calc(8rpx + env(...))`
  - `.tab-list`
    - `min-height` 从 `76rpx` 调整为 `66rpx`
  - `.tab-item`
    - `min-height` 从 `76rpx` 调整为 `66rpx`
  - `.tab-content`
    - `min-height` 从 `76rpx` 调整为 `66rpx`
    - `gap` 从 `6rpx` 调整为 `4rpx`
  - `.tab-bubble`
    - 尺寸从 `56rpx * 56rpx` 调整为 `50rpx * 50rpx`
    - 圆角从 `28rpx` 调整为 `25rpx`
  - `.tab-icon`
    - 尺寸从 `26rpx * 26rpx` 调整为 `24rpx * 24rpx`

## 11. 调整说明

- 本次按“整体纵向高度缩小”要求，联动收缩了外层、内层和圆圈尺寸。
- 保持当前横向位置和玻璃质感不变。
- 避免只压缩外层导致内部内容拥挤。

## 12. 验证记录

- 本次仅修改 `index.wxss`。
- 未修改结构、路由、逻辑和业务功能。

## 13. 回退指引

- 如需恢复本次修改前版本，可直接使用以下备份文件覆盖：
  - `g:\zhiximini\_local_backups\2026-04-21-112142-tabbar-height-reduce\index.wxss.bak`
